# 작업 인수인계 노트

Claude 또는 Codex에서 다음 작업을 시작할 때, 먼저 이 문서의 최신 항목을 읽는다.
완료한 작업·검증 결과·남은 결정 사항을 작업 단위로 아래 형식에 계속 추가한다.

```md
## YYYY-MM-DD — 작업 제목

- 상태: 진행 중 | 완료 | 보류
- 변경: 바꾼 내용과 관련 파일/커밋
- 검증: 실행한 명령·기기 확인 결과
- 다음: 다음 세션에서 할 일과 주의점
```

---

## 2026-08-18 — 1.6.0 개발 APK 실기 검증 준비

- 상태: 진행 중
- 브랜치: `feat/curation-meaning-lang`
- 원격 반영: `5ad017b chore: 작업 지침과 큐레이션 검토 결과 추가`까지 `origin/feat/curation-meaning-lang`에 푸시 완료.
- 현재 앱 버전: `app.json`은 아직 `1.5.0`이다. 개발 APK에서 버전 표기나 업데이트 소식이 1.6.0으로 보이지 않는 것은 정상이며, 1.6.0 출시 직전에 버전과 `constants/announcements.ts`를 함께 갱신해야 한다.

### 이번 APK에서 확인할 것

1. [x] 게스트로 AI 사용 후 로그아웃 → 다시 `바로 시작하기`: AI 사용량/한도가 0으로 초기화되지 않고 유지됨. **실기 통과(2026-08-18).**
2. 게스트 AI 실패 안내: 무조건 로그인 유도가 아니라 실제 실패 사유에 맞는 안내여야 한다.
3. [x] 설정의 광고 보상: **Google 계정으로 A·B·C 3상태 실기 통과(2026-08-18).** ⚠️ 이 항목은 원래 "1.6.0 서버 마이그레이션 후에야 하루 2회가 되므로 재검증"이라고 적혀 있었으나 **틀렸다** — 이미 적용된 `20260813020000_pro_3000_monthly_pool.sql:33-34`가 `is_anonymous` 여부로만 가르므로 **Google 로그인 Free 는 지금도 2회·회당 +20**이다. 하루 1회는 게스트 한정이다. 남은 것은 게스트가 2회가 되는 ④ 서버 적용 뒤의 재확인뿐이다.
4. 게스트 AI 한도 초과: 자동완성은 뜻만 남고 예문이 비며, AI 단어 생성과 사진 스캔은 광고 보상 흐름으로 이어져야 한다.
5. 공식 큐레이션 서버 시딩/공개가 끝난 경우: 목록 캐시, 게스트 가져오기, 1,000단어 이상 BSL 덱 전체 로드, 오프라인 안내를 확인한다.

### 주의할 배포 순서

- `supabase/migrations/20260818000000_remove_guest_tier.sql`은 **1.6.0 스토어 출시 후** 적용한다. 현재 서버에 적용하지 않았으면 게스트 등급이 `free`로 바뀌지 않아도 정상이다.
- 공식 큐레이션 앱 검증은 Supabase의 덱 시딩과 `is_published=true` 설정이 끝나야 가능하다. 세부 체크리스트는 `docs/curation-server-migration-spec.md` §9에 있다.

### 다음

- 개발 APK 실기 결과를 위 항목별로 이 문서에 기록한다.
- 발견한 버그는 재현 절차, 기기/빌드, 화면, 콘솔·서버 오류를 함께 기록한다.

### 2026-08-18 실기 관찰 — 최신 APK/번들 확인 필요

- 관찰: 게스트 진입 뒤 단어장 탭에서 기존 단어장이 잠시 보였다가 사라졌다. 설정의 한도 칩도 처음에는 `0/10`이었다가 `게스트 · 로그인하고 한도 받기`로 바뀌었다.
- 판정: `게스트 · 로그인하고 한도 받기` 문구는 현재 브랜치에 없다. 이 문구는 게스트 익명 Supabase 세션·10단어 한도 도입 전 코드에만 있던 값이며, 현재 소스의 문구는 `게스트 · 오늘 {{used}}/{{limit}}단어`다. 따라서 설치 APK가 오래된 내장 번들 또는 캐시된 개발 번들을 실행했을 가능성이 높다. 이 상태에서는 1.6.0 검증 결과로 사용하지 않는다.
- 단어장 원인 후보: 직전에 Google/Apple 로그인 상태였다면, 클라우드 계정의 로컬 단어장은 게스트 전환 시 계정 격리를 위해 제거되는 것이 맞다. 설정 화면은 `logout()`을 기다리지 않고 로그인 화면으로 이동하므로, 정리 작업이 끝날 때까지 이전 단어장이 잠깐 보였다가 사라질 수 있다. 이전에 클라우드 로그인한 적이 없는 순수 신규 게스트에서도 반복되면 별도 버그다.
- 다음 확인: APK의 build/version/커밋 또는 연결된 Metro 번들이 최신인지 먼저 확정한다. 그 뒤 앱을 완전히 종료하고 다시 열어, 순수 게스트 상태에서 단어장·한도 칩이 1분 이상 변하지 않는지 확인한다. 재현 시 전 단계가 `Google/Apple 로그인 → 로그아웃 → 게스트 시작`이었는지 함께 기록한다.
- 빌드 판단: 사용 중인 개발 APK는 2026-07-29 빌드다. 이후 네이티브 의존성 추가는 없어 Metro에 최신 소스를 `--clear`로 연결하면 JS 기능 확인은 가능하다. 그러나 `app.json` 버전·로케일 설정이 바뀌었고, 1.6.0 출시 후보의 내장 번들/릴리스 모드까지 검증하려면 최신 소스로 새 `preview` APK를 빌드해야 한다. 현재 7/29 APK에서 나온 결과는 1.6.0 최종 검증으로 인정하지 않는다.
- 재확인: 최신 번들에 다시 연결한 뒤 설정의 게스트 한도 문구 `게스트 · 오늘 0/10단어`가 정상 표시됨. 앞선 옛 문구는 잘못된 APK/번들에서 확인한 것으로 판단한다.
- 실기 통과: 게스트로 단어 자동완성/AI 사용 → 설정에서 로그아웃 → 다시 `바로 시작하기` 후에도 이전 AI 사용량과 한도가 그대로 유지됐다. `86695c3`의 익명 세션 유지 수정 검증 완료.

### 1.6.0 공개 후 서버 마이그레이션 순서

- 목표 순서: 1.6.0 앱과 스토어 설명이 **Play와 App Store 양쪽 사용자에게 도달한 것을 확인한 뒤** `supabase/migrations/20260818000000_remove_guest_tier.sql`을 적용한다. 서버 함수는 전 플랫폼 공통이라 한쪽만 공개됐을 때 적용하면 아직 구버전인 다른 플랫폼의 문구·동작이 어긋난다.
- 적용 후 익명 게스트 RPC도 `tier='free'`를 반환한다. 생성 24시간 이내 계정은 300단어, 이후 계정은 50단어, 광고는 +20단어씩 하루 2회다. 앱의 계정 정체성은 계속 게스트/로컬 저장이며, 한도 등급만 Free로 표시된다.
- 🔴 적용 시 일반 `supabase db push`를 쓰면 대기 중인 이 마이그레이션이 의도보다 먼저 함께 나갈 수 있다. 출시 전 다른 DB 변경은 파일 단독 적용 + migration repair 절차를 사용한다.
- 새로 발견한 출시 전 잔여 작업: `i18n/locales/ko.json` FAQ·약관에 `게스트 10단어`, `게스트 광고 1회 +10` 등 현재 정책 문구가 아직 남아 있다(영어·스페인어 대응 키도 함께 점검 필요). 1.6.0 앱에서 새 정책 문구로 고친 뒤 배포해야 하며, 현 소스 그대로 서버 마이그레이션을 적용하면 인앱 설명이 거짓이 된다.
- 서버 적용 직후 검증: 익명 UUID로 `select * from ai_effective_plan('<uuid>')` 실행 → `tier='free'`, `day_limit=300 또는 50`, `reward_amount=20`, `reward_max_views=2` 확인. 기기에서 한도 새로고침 후 `무료 · 오늘 ...` 표시와 광고 보상 수치를 확인한다.

### 발견 버그 — 게스트 → Google → 로그아웃 → 게스트에서 한도 리셋

- 상태: **코드 수정 완료 · 실기 검증 대기 · 1.6.0 서버 마이그레이션 전 확인 필요**
- 실기 재현(2026-08-18): 게스트 한도+광고 보너스 20단어 소진 → Google 로그인 → Google 로그아웃 → `바로 시작하기` → 기존 게스트 단어장이 보이지 않고 AI 한도가 0으로 리셋됨.
- 단어장 판정: 현재 설계상 예상된 계정 격리 동작이다. 게스트 단어장은 Google 첫 로그인 때 로컬에서 클라우드로 업로드되고, Google 로그아웃이 성공하면 `features/auth/store.ts:logout()`이 클라우드 계정 데이터를 로컬에서 지운다. 따라서 다시 게스트로 시작했을 때 이전 단어장이 보이면 오히려 계정 데이터 노출이다. 이전 단어장은 같은 Google 계정으로 다시 로그인하면 클라우드에서 복원돼야 한다(별도 확인 필요).
- 한도 판정: **의도와 맞지 않는 우회 버그다.** 직접 게스트 로그아웃은 익명 세션을 보존하지만, 게스트 상태에서 `signInWithGoogle()`이 `signInWithIdToken()`을 호출하면 익명 세션이 Google 세션으로 교체된다. Google 로그아웃 뒤 `loginAsGuest()`에는 재사용할 익명 세션이 없어 `signInAnonymously()`로 새 UUID를 만든다. 사용량은 UUID별이라 0으로 시작한다.
- 영향: 1.6.0 서버 정책 적용 뒤에는 이 경로로 새 게스트의 첫 24시간 300단어를 반복 획득할 수 있다. 스펙이 막으려던 `로그아웃 → 재진입`보다 탭 몇 번만 더 필요한 손실 없는 우회라 출시 전 해소해야 한다.
- 테스트 공백: `__tests__/auth-guest-anonymous-session.test.ts`는 `게스트 → 로그아웃 → 게스트`만 검증하며, `게스트 → Google → 로그아웃 → 게스트` 세션 연속성은 검증하지 않는다.
- 공식 권장과 차이: Supabase는 익명 사용자를 영구 사용자로 전환할 때 OAuth `linkIdentity()`를 사용해 같은 사용자 ID에 신원을 연결하도록 안내한다. 현재 앱은 직접 `signInWithIdToken()`으로 별도 Google 세션에 로그인한다. 다만 identity linking만으로도 Google 로그아웃 후 다시 익명으로 접근할 수 있는 것은 아니므로, 수정안은 데이터 승격·기존 Google 계정 충돌·게스트 세션 복원 정책까지 함께 설계해야 한다.

#### 권장 수정안

- **게스트 익명 세션의 refresh token을 SecureStore에 별도 보관하고 복원한다.** Google/Apple 로그인 직전에 현재 세션이 익명이면 토큰을 저장한다. 클라우드 로그아웃 후 `바로 시작하기`에서는 새 익명 계정을 만들기 전에 저장한 refresh token으로 `supabase.auth.refreshSession({ refresh_token })`을 호출해 원래 익명 UUID를 복원한다.
- 복원 성공 시 refresh token이 회전하므로 반환된 새 토큰을 즉시 SecureStore에 덮어쓴다. 게스트 세션의 `TOKEN_REFRESHED` 이벤트에서도 최신 토큰을 저장한다.
- 저장 토큰이 없거나 서버에서 무효라면 그때만 `signInAnonymously()`로 새 게스트를 만들고 새 토큰을 저장한다.
- `deleteAccount()`처럼 기기 흔적 전체 삭제를 약속한 경로에서는 보관 토큰도 삭제한다. 일반 게스트 로그아웃과 Google 로그아웃에서는 삭제하지 않는다.
- 단어장은 현행 유지: 게스트 데이터가 Google로 업로드된 뒤 Google 로그아웃하면 로컬에서 지우고, 다시 게스트로 돌아와도 노출하지 않는다. 복원 대상은 익명 서버 신원/한도이며 단어 데이터가 아니다.
- 이 안을 고른 이유: 기존 Google 계정 로그인·첫 로그인 합치기·Apple 로그인을 그대로 유지하고 서버 DB 변경이 없다. `linkIdentity()`는 신규 Google 신원 승격에는 정석이지만 기존 Google 계정 충돌과 데이터 병합 정책을 다시 설계해야 하며, Google 로그아웃 후 익명 접근도 별도로 해결해야 한다. 기기 ID 기반 서버 한도는 서버 함수와 모든 Edge 호출 계약을 바꾸고 조작 가능한 새 식별자를 추가하므로 과하다.
- 필수 회귀 테스트: (1) 게스트 A → Google → 로그아웃 → 게스트가 A의 UUID/사용량 복원, (2) 회전 토큰 재저장, (3) 무효 토큰이면 새 익명 계정 폴백, (4) 직접 게스트 로그아웃 유지, (5) 늦게 끝난 Google 로그아웃이 복원된 게스트를 지우지 않음, (6) 계정 삭제 시 보관 토큰 삭제.

#### 2026-08-18 구현 결과

- `features/auth/guest-session-vault.ts` 추가: 익명 refresh token을 SecureStore에 저장하고, `refreshSession({ refresh_token })`으로 복원하며, 성공 시 회전 토큰을 즉시 덮어쓴다.
- `features/auth/store.ts` 연결: 기존 게스트 hydrate·신규 게스트 생성·익명 `TOKEN_REFRESHED`·Google/Apple 로그인 직전·클라우드 로그아웃 뒤 게스트 재진입·계정 삭제 경로를 처리한다.
- 영구 무효 오류(`refresh_token_not_found`, `refresh_token_already_used`, `session_not_found`, `session_expired`)에서만 저장 토큰을 지우고 새 게스트 생성을 허용한다. 네트워크 오류에서는 토큰을 보존하고 새 UUID를 만들지 않는다.
- 자동 검증: 인증 관련 4개 suite, 12개 test 통과. 전체 Jest는 80개 suite 중 77개 통과·테스트 1,064개 전부 통과했으며, 실행 전 환경 오류 3개만 남음(`sqlite3` 미설치 2개, 기존 photo-import 테스트의 React Native ESM 변환 설정 1개). 변경 파일 ESLint 오류 0개(`require`/import-order 기존 테스트 경고 11개만 존재). `git diff --check` 통과.
- 전체 TypeScript 검사는 이번 변경과 무관한 기존 오류 2건 때문에 완료 불가: `scripts/fetch-wiktionary.ts:95` 구문 오류, `supabase/functions/verify-purchase/handler.ts:13`의 Deno `.ts` import 설정 오류.
- 남은 실기: 사용량을 만든 게스트 A → Google 로그인 → Google 로그아웃 → `바로 시작하기` → 설정에서 A의 기존 사용량/광고 횟수가 유지되는지 확인한다. 단어장은 게스트에 노출되지 않고 같은 Google 계정 재로그인 시 복원되는 것이 정상이다.

### 2026-08-18 추가 실기 관찰 — Google 단어장 재노출 가능성

- 관찰: Google 로그아웃 후 게스트 진입 시, 처음에는 단어장이 없다가 잠시 뒤 과거(전날) 게스트/Google 전환 시점의 단어장이 다시 나타났다. 같은 시점 설정 한도는 `게스트 · 오늘 0/20단어`로 표시됐다.
- 한도 판정: 현재 서버의 게스트 정책은 기본 10단어 + 광고 1회 보상 10단어(하루 최대 1회)다. 따라서 사용량 0, 광고 1회 시청 후 `0/20` 표시는 저장된 익명 UUID의 보상 상태와 정확히 일치한다. 즉, 한도 측면에서는 새 guest-session-vault 복원이 동작했을 가능성이 높다. 1.6.0 서버 마이그레이션 후에는 첫 24시간에는 기본 300 + 광고 20(첫 시청 뒤 `0/320`), 이후에는 기본 50 + 광고 20(첫 시청 뒤 `0/70`)이므로 별도 재검증한다.
- 단어장 판정: **비정상(계정 격리 누출)**. guest-session-vault는 Supabase Auth refresh token만 복원하며 SQLite 단어장을 복원하지 않는다. 게스트가 Google 로그인 과정에서 올린 단어장은 Google 계정 데이터이므로, Google 로그아웃 뒤 게스트 화면에 보여서는 안 된다.
- 코드상 원인: `features/vocab/use-bootstrap.ts`가 Google 상태에서 `loadCloudData()`를 백그라운드로 시작한다. 로그아웃/게스트 전환으로 훅이 정리되어도 `cancelled`는 화면 무효화만 막고 네트워크 작업을 취소하지 않는다. 이어 `features/sync/engine.ts:pullChanges()`는 시작 시에만 `isCloudAuthed()`를 검사하고, 원격 fetch가 끝난 뒤 `runInTransaction()` 전에 현재 사용자/세션을 재확인하지 않는다. 따라서 `logout()`의 `clearAllData()` 뒤에도 이미 시작된 Google pull이 목록·단어를 다시 SQLite에 INSERT할 수 있다.
- 권장 다음 수정(아직 구현 전): pull 시작 시 cloud user id/session 세대(epoch)를 캡처하고, 원격 fetch 완료 직후 및 SQLite 트랜잭션 직전에 현재 cloud user id와 일치하는지 재확인한다. 다르면 쓰기·워터마크 갱신 없이 취소한다. `use-bootstrap`의 백그라운드 pull이 로그아웃 뒤 완료되는 회귀 테스트도 추가한다. 단순 React effect `cancelled`만으로는 이미 시작한 요청의 DB 쓰기를 막을 수 없다.

#### 2026-08-18 수정 결과

- `features/sync/engine.ts`: `pullChanges()`가 시작한 cloud user id를 캡처하고, 원격 fetch 직후·보조 fetch 뒤·SQLite 트랜잭션 진입 직전에 현재 cloud user id를 다시 확인하도록 변경했다. 로그아웃/게스트 전환/다른 계정 전환이면 stale pull은 DB 쓰기와 watermark 변경 없이 종료한다.
- 새 회귀 테스트 `__tests__/sync-pull-session-guard.test.ts`: Google pull의 네 원격 요청을 의도적으로 멈춘 뒤 guest로 상태를 전환하고 응답을 풀어 준다. 수정 전에는 SQLite transaction이 1회 실행되어 실패했고, 수정 후 0회로 통과한다.
- 검증: `pnpm test -- --runInBand sync-pull-session-guard.test.ts auth-guest-anonymous-session.test.ts auth-hydrate-once.test.ts auth-logout-late-clobber.test.ts guest-session-vault.test.ts` → 5 suites, 13 tests 통과. 변경 파일 ESLint는 오류 0개(기존 테스트 import/require 경고 12개), `git diff --check` 통과.
- 실기 재검증: 최신 Metro 번들/새 APK에서 Google 계정에 확실히 구별되는 단어장을 만든 뒤 Google 로그아웃 → 즉시 guest 시작 → 단어장 탭을 30초 이상 관찰한다. Google 단어장은 나타나지 않아야 하며, 게스트 A 한도/광고 이력만 유지되어야 한다. 이어 같은 Google 계정 재로그인 시 Google 단어장이 정상 복원되는지 확인한다.

#### 추가 원인 및 수정 — 로그아웃 정리 완료 전 guest 진입

- 실기에서 Google 전용 단어장이 guest에 보인다는 보고가 이어졌다. `app/(tabs)/settings.tsx`의 로그아웃 확인 동작이 `logout()`을 await하지 않고 즉시 `/login`으로 이동하는 것이 별도 경쟁 상태였다. 사용자가 로그인 화면에서 곧바로 guest를 시작하면 `logout()`의 `clearAllData()`가 아직 진행 중이라, 삭제 전 Google 단어장이 guest 화면에 노출될 수 있다.
- `app/(tabs)/settings.tsx`를 수정해 `await logout()`이 끝난 뒤에만 `/login`으로 이동하도록 했다. 이로써 guest 시작 버튼은 계정 데이터 정리 완료 전에는 접근할 수 없다. 앞의 stale-pull guard와 함께 (1) 기존 로컬 데이터가 정리 전 보이는 문제, (2) 이미 시작된 원격 pull이 나중에 다시 쓰는 문제를 각각 막는다.
- 재검증: 관련 인증/동기화 4 suites, 12 tests 통과. ESLint 오류 0개(기존 미사용 변수와 테스트 import-order 경고만 존재), `git diff --check` 통과.

#### 실기 통과 — 게스트 ↔ Google 계정 격리와 게스트 한도 복원

- 재현/검증(2026-08-18): 게스트 A에서 단어 2개 추가·AI 사용량 `2/20` 확인 → Google 로그인 후 Google 단어장 생성·단어 2개 추가(기존 Google 사용량 포함 `6/50`) → Google 로그아웃 → 다시 guest A 시작.
- 결과: guest 화면에는 이전 guest 단어장(이미 Google 계정으로 동기화된 데이터)이 나타나지 않았고, Google 단어장도 노출되지 않았다. 반면 guest A의 AI 사용량은 `2/20`으로 정확히 유지됐다.
- 판정: **통과.** 단어 데이터는 현재 로그인 계정과 격리되고, 별도 보관한 익명 refresh token으로 quota identity만 이어진다. 이어 같은 Google 계정에 재로그인했을 때 해당 계정의 단어장이 클라우드에서 복원되는지 확인하면 이 흐름의 마지막 검증이 된다.

#### 실기 통과 — 게스트 광고 보상 흐름

- 검증(2026-08-18): 현재 개발 서버의 게스트 정책(기본 10단어, 광고 1회 +10단어, 하루 최대 1회)에서 광고 보상 진입·시청 완료·한도 반영·소진 상태 안내까지 전체 흐름을 확인했다.
- 결과: **통과.** 1.6.0 서버 마이그레이션 적용 후에는 보상 횟수/수치가 하루 2회·회당 +20단어로 변경되므로, 공개 후 해당 수치와 두 번째 광고 흐름을 다시 실기 검증한다.

#### 실기 통과 — 설정 광고 혜택 줄 4상태 (`4293fed`)

- 검증(2026-08-18, dev APK · **Google 로그인** · AdMob 키 미설정이라 구글 테스트 광고).
- 결과: **A·B·C 통과.** 광고 재생·보상 지급(EARNED_REWARD → `grant_rewarded_bonus` → quota 갱신)도 정상 동작했다.
  - A 시청 전 — `광고 보고 혜택 받기 / AI 자동완성 20개 + 24시간 배너 제거`
  - **B 1회 시청 — `광고 없이 학습 중 / <시각>까지 · 한 번 더 보면 +20단어`** ← 이번 커밋이 고친 자리
  - C 2회 시청 — `광고 없이 학습 중 / <시각>까지 · 오늘 혜택은 다 받았어요` · 누를 수 없음
- B 가 이 수정의 본체다. 고치기 전에는 제목·부제가 각자 `ad_free_until` 한 축만 봐서, 1회만 본 사용자에게
  `광고 없이 학습 중 / <시각>까지`만 뜨고 **남은 1회(+20단어)가 어디에도 보이지 않았다.** 받을 수 있는 것을
  모른 채 지나가는 실질 손해였다.
- 🔴 **게스트로는 B 를 밟을 수 없다**(하루 1회라 A → C 로 건너뛴다). 앞서 "게스트 광고 보상 흐름 통과"로
  기록된 검증은 A·C 만 지난 것이며, **B 는 이번에 Google 계정으로 처음 확인됐다.** 상태를 나눠 세지 않고
  "광고 흐름 통과"로 묶으면 고친 자리를 안 밟고도 통과로 보인다.
- [ ] **D 상태는 미검증** — 광고 소진(`rewarded_views` 상한) + `ad_free_until` 없음. 시청 횟수의 KST 자정
  초기화가 `ad_free_until`(24h) 만료보다 항상 먼저 오므로 **정상 경로로는 도달하지 않는다.** 서버가
  `ad_free_until` 을 주지 못한 예외에서만 드러나며, 확인하려면 `ai_usage_daily` 행을 직접 세팅해야 한다.
  방어 문구(`오늘 광고 혜택을 다 받았어요 / 한국 시간 자정에 다시 볼 수 있어요`)는 테스트로만 고정돼 있다.
- [ ] **함께 바뀐 문구 2건 미확인**: (1) 한도 소진 모달 본문 `ads.rewardedBody` 에 추가한
  `24시간 동안 배너 광고도 사라져요`, (2) 광고 성공 직후 `ads.rewardedGrantedBodyMore`
  (`한 번 더 보면 +20단어를 더 받을 수 있어요`, 1회차에만). 다음 실기에서 함께 본다.
- 상태를 바꾼 뒤에는 앱을 완전히 종료 후 재시작해야 한다. 설정 탭 진입 시 `refresh` 는 `force=false` 라
  90초 캐시(`STALE_MS`)에 막힌다.

### 2026-08-18 검토 — 사진 스캔 한도 정책 및 UX

- 확정 정책: 사진 스캔의 **이미지 추출은 무차감**이며, 추출된 각 단어를 AI 보강할 때만 단어당 1씩 차감한다. 과거에 문서화된 `사진 1장 추출 오버헤드 5단어` 정책은 적용하지 않는다.
- 현재 서버 구현 확인: `supabase/functions/scan-image/index.ts`는 `get_ai_quota_status`로 잔량만 확인하고 차감하지 않는다. 잔량이 0이면 `extractWordsFromImage`(Vertex 분석)를 호출하기 전에 `429 quota_exceeded`를 반환한다. 따라서 한도 0에서는 사진에서 몇 단어가 있는지 추출·분석하지 않는 것이 정상이다.
- 실기 관찰: AI 한도 1개가 남은 상태에서 8개 후보를 찾았고 카드 3개 및 `광고 보고 이어서 채우기 · 5개 남음` 버튼이 보였다. 버튼을 누르면 `오늘의 단어 한도를 모두 사용했어요`가 나왔다.
- 판정 1 — 카드 수: `PhotoImportWorkflow`는 추출 응답 직후 현재 quota store의 잔량만큼 카드 수를 자른다. 카드가 3개 보였다는 것은 **그 시점 클라이언트 quota store가 3개 남음으로 읽혔다**는 뜻이며, 실제 서버 한도 1과 불일치할 수 있다. 스캔 시작 직전에 quota를 강제 새로고침하거나, `scan-image` 성공 응답의 서버 quota를 유일한 기준으로 전달해 첫 페이지를 자르도록 수정할 필요가 있다. AI 보강 실패 카드도 사용자가 수동으로 저장할 수 있으므로, 카드 수가 곧 AI 차감 수는 아니지만 현재 표시는 오해를 만든다.
- 판정 2 — 광고 문구: 현재 버튼 라벨은 잔여 AI 한도 0이면 무조건 `광고 보고 이어서`를 표시하지만, 클릭 시 `rewarded.canWatch`가 false(오늘 광고를 이미 모두 시청)면 "오늘 혜택 소진" 안내만 띄운다. 광고 가능 여부를 라벨/버튼 상태에 반영하지 않아 잘못된 약속이다.
- 권장 수정(아직 구현 전): (1) `scan-image` 응답 quota로 첫 카드 수를 정확히 제한, (2) 광고 잔여 시에만 광고 버튼·보상 후 이어가기, (3) 광고 잔여가 없으면 `오늘 한도 소진`과 Pro 업그레이드 CTA를 표시, (4) 한도 0에서 스캔을 시작할 때도 같은 인라인 광고/Pro 안내를 제공한다. 카드 생성과 서버 차감을 보장하는 회귀 테스트를 추가한다.

#### 추가 분석 및 제품 결정 — 한도 밖 사진 단어는 저장 불가

- 실기 관찰: 한도 1에서 표시된 3개 카드 중 하나는 한국어 뜻이 없고 영어 정의·발음·영어 예문만 있었다.
- 원인 확인: `lib/translation-api.ts`는 Edge `quota_exceeded` 뒤에도 영어 출발어면 `dictionaryapi.dev` 폴백을 수행한다. 이 사전은 한국어 뜻을 제공하지 않으므로, 해당 카드의 형태와 정확히 일치한다. 현재 사진 스캔이 일반 자동완성과 같은 폴백 정책을 공유해서 발생한다.
- 확정 UX 원칙: 사진 스캔에서는 **서버 한도 안에서 AI 보강 가능한 단어만 카드로 만들고 저장을 허용한다.** 한도 밖 후보는 `pendingTerms`로만 보관하며 직접 편집·저장할 수 없다. 따라서 AI 한도 1이면 카드·저장 가능 단어도 정확히 1개이고, 나머지는 "N개 더 채우기" 대기 목록이다.
- 이어가기: 광고 보상이 남으면 광고 시청 뒤 받은 잔량만큼 대기 목록을 카드화·AI 보강한다. 광고 보상도 없으면 광고 약속 대신 `오늘 AI 한도를 모두 사용했어요`와 Pro 업그레이드 CTA를 표시한다. 한도 0에서 스캔 시작 시에는 이미지 추출을 실행하지 않고 같은 인라인 보상/Pro 안내를 제공한다.
- 구현 주의: 사진 모드의 `quota_exceeded`는 영어 사전 폴백으로 넘기지 말고 큐에 한도 중단 신호로 전달해야 한다. 동시 요청·다른 기기의 사용으로 서버 한도가 중간에 소진되어도, 그 이후 항목은 카드/저장 대상으로 승격되지 않게 해야 한다.

#### 2026-08-18 구현 결과 — 사진 스캔 한도 엄수

- `components/PhotoImportWorkflow.tsx`: 운영자 키 경로에서 quota 상태가 없으면 카드 생성을 보류하고, `scan-image` 응답이 store에 반영한 최신 서버 잔량만큼만 첫 카드를 만든다. 광고가 남아 있을 때만 "광고 보고 이어서"를 보이며, 광고를 모두 썼으면 버튼을 `Pro 업그레이드`로 바꾸고 누르면 한도 소진 안내와 요금제 이동을 제공한다.
- `lib/translation-api.ts` / `lib/types.ts`: 사진 모드의 `quota_exceeded`는 영어 사전 폴백 대신 `photoQuotaExceeded` 신호를 돌려준다. 사진 워크플로는 해당 카드를 저장 가능 목록에서 제거하고 대기 목록으로 되돌린다. 따라서 한도 밖 단어는 부분 정보 카드나 수동 저장 대상으로 남지 않는다.
- 검증: `pnpm test -- --runInBand gemini-api.test.ts sync-pull-session-guard.test.ts` → 2 suites, 8 tests 통과. 변경 파일 ESLint 오류 0개(기존 `useEffect` 의존성 경고 1개), `git diff --check` 통과.
- 남은 실기: 한도 1·8개 후보 사진에서 AI 카드/저장 가능 단어가 정확히 1개인지, 나머지 7개가 광고/Pro 대기 목록인지 확인한다. 광고 소진 상태에서는 광고 문구 대신 Pro 업그레이드가 보여야 한다.

#### 추가 실기 관찰 — 한도 0 사진 스캔의 오류 표현

- 관찰: 한도 0에서 사진 선택 → 분석하기를 누르면 한도 안내 뒤 개발 오류 오버레이 `ScanError: quotaExceeded`가 보이고, 사용자 Alert 제목도 `오류`로 표시된다.
- 원인: `components/PhotoImportWorkflow.tsx`의 `processImage` catch가 `console.error(error)`를 호출해 개발 빌드의 오류 오버레이를 유발한다. 또한 제목을 BYOK 한도(`byokQuotaExceeded`, `byokPerMinuteQuota`)에만 `scanError.quotaTitle`로 지정하고 운영자 한도 `quotaExceeded`는 `common.error`로 떨어뜨린다.
- 판정/다음 수정: 운영자 `quotaExceeded`도 한도 제목(예: `AI 단어 한도에 도달했어요`)으로 표시하고 `console.error` 대상에서 제외한다. 더 나아가 분석 버튼 직전에 잔량 0을 검사해 Edge 호출·예외 처리 대신 인라인 광고/Pro 안내로 바로 보낸다. 프로덕션 APK에는 개발 오류 오버레이가 없지만, `오류` 제목은 그대로 노출되므로 출시 전 수정 필요.

#### 2026-08-18 후속 구현 — 한도 0 사진 스캔 진입 차단

- `components/PhotoImportWorkflow.tsx`: 운영자 `quotaExceeded`를 BYOK 한도와 동일하게 `scanError.quotaTitle`(한국어: `사용량 한도에 도달했어요`)로 표시하도록 수정했다. 정상 한도 소진에는 `console.error`를 호출하지 않아 개발 빌드의 `ScanError: quotaExceeded` 오류 오버레이가 더 이상 뜨지 않는다. 실제 네트워크·응답 오류만 로그로 남긴다.
- `app/add-word.tsx`: 사진 스캔의 카메라·갤러리 버튼(`openPhotoScan`)을 누르는 시점에 quota store의 잔량을 확인한다. 운영자 키 경로이고 잔량이 0이면 사진 권한·사진 선택·Edge 호출로 들어가지 않고 `notifyQuotaExceeded()`를 즉시 호출한다. 따라서 일반 화면에서 안전하게 전역 광고 보상 또는 Pro 한도 안내를 보여준다.
- UX 판정: 사용자가 한도를 이미 알고 있는 상황에서 사진을 고르고 `분석하기`를 다시 누르게 하지 않는다. 사진 선택 전 차단이 맞으며, 서버 `scan-image`의 잔량 검사도 방어 계층으로 유지한다.
- 검증: `app/add-word.tsx` 및 `PhotoImportWorkflow.tsx` ESLint 오류 0개, `git diff --check` 통과. 두 파일에는 이번 변경 전부터 존재한 import/hook 의존성 경고가 남아 있다.

### 현재 출시 전 남은 실기 검증 요약

1. **Google 재로그인 복원:** guest → Google → 로그아웃 → guest 격리/한도 유지(통과) 뒤, 같은 Google 계정으로 재로그인했을 때 Google 단어장과 승격된 guest 단어장이 클라우드에서 정상 복원되는지 확인.
2. **사진 스캔 한도:** 한도 1·후보 8개 사진에서 AI 카드·저장 가능 단어가 정확히 1개인지, 나머지 7개가 대기 목록인지 확인. 광고 잔여 시 광고 보상 후 이어지는지, 광고 소진 시 Pro 업그레이드 문구인지 확인. 한도 0이면 사진 버튼을 누르는 즉시 광고/Pro 안내가 뜨고 카메라·갤러리가 열리지 않는지 확인.
3. **기타 AI 한도 초과:** 자동완성의 기본 결과 처리, AI 단어 생성의 광고 보상 후 재시도 흐름 확인.
4. **1.6.0 공개 후 서버 적용:** Play/App Store 양쪽 공개 확인 후 `20260818000000_remove_guest_tier.sql`을 적용하고, 첫 24시간 300단어·광고 하루 2회/회당 +20단어·두 번째 광고/자정 초기화를 검증.
5. **공식 큐레이션:** 서버 시딩·공개 후 목록 로드, guest 가져오기, 대용량 BSL 덱, 오프라인 안내 확인.

### UX 검토 대기 — 첫 Google 로그인에서 "클라우드 유지" 확인

- 현재 `features/vocab/use-bootstrap.ts`의 첫 로그인 충돌 선택은 `합치기`와 `클라우드 유지`를 제공한다. `클라우드 유지`를 누르면 `applyFirstLoginCloudReset()`이 guest 로컬 SQLite(단어장·단어·학습 기록)를 비우고 클라우드 데이터로 교체한다.
- 문제: 게스트 데이터는 로컬 전용일 수 있어 이 결정은 되돌릴 수 없는 삭제인데, 현재 첫 Alert에서 곧바로 실행된다.
- 권장 UX: 사용자가 `클라우드 유지`를 누르면 두 번째 destructive Alert를 표시한다. 제목 예: `이 기기의 단어장을 지울까요?`, 본문: `게스트로 저장한 단어장과 학습 기록이 이 기기에서 삭제되고 클라우드 단어장으로 교체됩니다. 이 작업은 되돌릴 수 없어요.` 버튼은 `취소`와 명시적 destructive 동사 `클라우드로 교체`를 쓴다. 두 번째 Alert에서 취소하면 첫 선택 화면으로 돌아가 `합치기`를 다시 고를 수 있어야 한다.

#### 2026-08-18 구현 결과 — 클라우드 유지 삭제 재확인

- `features/vocab/use-bootstrap.ts`: 첫 로그인 충돌 선택을 `chooseConflictResolution()`으로 분리했다. `클라우드 유지` 선택 뒤 두 번째 destructive Alert를 표시하며, 취소 시 첫 선택 Alert로 되돌아가 합치기/클라우드 유지를 다시 고를 수 있다. `클라우드로 교체`를 명시적으로 눌러야만 `applyFirstLoginCloudReset()`이 실행된다.
- 번역 추가: `i18n/locales/ko.json`, `en.json`, `es.json`에 삭제 영향과 되돌릴 수 없음을 알리는 제목·본문·확인 버튼 문구를 추가했다.
- 검증: 세 locale JSON 파싱 통과, `pnpm test -- --runInBand auth-hydrate-once.test.ts` 통과. `use-bootstrap.ts` ESLint 오류 0개(기존 `user.nickname` hook 의존성 경고 1개).
- 남은 실기: 첫 Google 로그인 충돌 상태에서 `클라우드 유지` → 재확인 `취소` → 첫 선택으로 복귀 → `합치기`가 정상 동작하는지, 그리고 `클라우드로 교체` 확정 시에만 guest 로컬 단어장·학습 기록이 지워지고 cloud 데이터가 로드되는지 확인.

#### 최신 실기 결과 (2026-08-18)

- [x] **Google 재로그인 복원:** guest → Google → 로그아웃 → guest 흐름 후 같은 Google 계정 재로그인 시, Google 단어장과 승격된 guest 단어장이 정상 복원됨.
- [x] **첫 로그인 충돌 재확인:** `클라우드 유지` → 재확인 취소 → 첫 선택 복귀, `합치기`, `클라우드로 교체` 확정 흐름이 모두 정상 동작함.
- [x] **사진 스캔 한도 0:** 사진 버튼을 누르는 즉시 광고/Pro 안내가 보이고 카메라·갤러리가 열리지 않음.
- [x] **사진 스캔 광고 소진:** 광고 문구 대신 Pro 업그레이드 흐름이 정상 표시됨.
- [ ] **사진 스캔 한도 1·후보 다수:** AI 카드·저장 가능 단어가 정확히 남은 한도(1개)만 생성되고 나머지가 대기 목록으로 남는지 확인은 다음 실기에서 진행.

#### 후속 실기 및 UX 다듬기 — 사진 스캔 Pro CTA

- 실기(2026-08-18): AI 한도 2개가 남은 상태에서 후보 6개 사진을 스캔했을 때 AI 보강·저장 가능 단어가 정확히 2개만 생성됐다. 나머지 4개는 대기 목록으로 남았다. **한도 엄수 동작 통과.**
- UX 관찰: 광고 보상이 소진된 상태에서 하단 CTA가 단순 `Pro 업그레이드`라, 대기 중인 4개 단어를 이어 볼 수 있다는 맥락이 약했다.
- 수정: `PhotoImportWorkflow`의 CTA를 `Pro로 업그레이드하고 {{count}}개 더 보기`로 변경했고, 영어·스페인어 번역도 함께 추가했다. CTA를 누르면 기존처럼 한도 소진 안내 뒤 요금제 화면으로 이동한다.
- 검증: 세 locale JSON 파싱 통과, 변경 컴포넌트 ESLint 오류 0개(기존 hook 의존성 경고 1개), `git diff --check` 통과.

## 2026-08-18 — Reddit 베타 피드백 검토 및 다음 작업

- 상태: 검토 완료 · 개선 작업 대기
- 사용자 피드백 원문 요약:
  - 영어 온보딩의 `Next`가 `Nex`로 보였다고 제보.
  - `Advanced Korean 500`이 TOPIK 5–6용이라고 설명하지만 `그녀`, `새롭다`, `피하다`, `과정` 등 초·중급 단어가 다수 포함되어 난이도가 지나치게 섞여 있다는 지적.
  - `그러다` 카드의 예문이 표제어 대신 `그렇게`를 사용해 실제 용례가 맞지 않는다는 지적.
  - 상세 화면의 `Korean definition`이 `English meaning`과 같은 영어 문구를 반복한다는 지적.
  - 여러 의미가 나열된 단어에 예문이 하나뿐이어서 각 의미의 용례를 알기 어렵다는 지적.
  - 예문 안의 단어·문법을 눌러 새 카드로 만드는 기능 제안.
  - BYOK 키를 입력했지만 AI 덱 생성이 오류로 실패했다는 제보. 정확한 오류 메시지·기기·OS는 아직 받지 못함.
  - 아보카도 디자인과 전체 앱의 가능성에는 매우 긍정적인 평가를 받음. 다른 채소 캐릭터와 다양한 표정 제안도 있었으나, 앱이 더 성장한 뒤 검토할 장기 아이디어로 답변함.

### 코드·데이터 확인 결과

- `i18n/locales/en.json`의 온보딩 버튼은 현재 `Next`로 정상이다. `Nex`는 현재 소스의 오타로 재현되지 않았으며, 제보자가 받은 빌드·폰트 렌더링·클리핑 여부를 preview APK에서 실기 확인해야 한다.
- `constants/curationData.ts`의 `Advanced Korean 500`에 실제로 `그녀`, `그러다`, `정부`, `새롭다`, `과정`, `피하다` 등이 들어 있다. 덱 설명은 `TOPIK II 5-6`을 명시하므로 난이도/표제어 선정에 대한 제보가 타당하다.
- `그러다` 항목의 예문은 `그가 그렇게 갑자기 태도를 바꿀 줄은...`으로, `그러다`의 활용형을 직접 사용하지 않는다. AI 프롬프트는 표제어나 활용형을 실제 예문에 포함하도록 요구하지만, 이 큐레이션 데이터는 그 조건을 위반한다.
- 해당 덱의 `definition`과 `meaningKr`가 동일한 영어 문구로 채워져 있다. 상세 화면은 source language 기준 `Korean definition`에 `definition`, target language 기준 영어 뜻에 `meaningKr`를 표시하므로 두 섹션이 영어를 중복 표시하는 현상이 실제 데이터에서 재현된다.
- 현재 Word 모델은 상위 필드 기준 예문 한 쌍을 저장할 수 있고, 자동완성의 `senses[]`는 뜻별 예문을 별도로 담을 수 있다. 그러나 큐레이션 덱 데이터에는 여러 뜻을 쉼표로 나열하면서 예문 하나만 둔 항목이 존재할 수 있어 의미별 용례 대응이 보장되지 않는다.
- `senses` 로직(`lib/senses.ts`)은 배열 개수·번호·필드 조립·저장 길이 같은 구조적 일치는 방어하지만, 뜻과 예문이 의미상 올바르게 대응하는지는 자동 검증하지 않는다. AI가 뜻을 잘못 분리하거나 sense ①/②의 예문을 뒤섞는 문제는 여전히 가능하다. 코드 주석에도 과거 상위 텍스트와 `senses[]` 불일치 실측 사례가 기록되어 있다.
- AI 덱 생성 관련 정적/회귀 테스트는 통과했다: `curation-ai-error-presentation`, `generate-prompt-legacy-field-sync`, `gemini-model-sync`, `gemini-quota-classify`, `duplicate-word-prevention` — 5 suites, 35 tests 통과. 따라서 현재 증거만으로 전체 AI 생성 기능 장애라고 단정할 수 없다. 실제 BYOK 키·네트워크·Gemini 오류 응답을 포함한 실기 재현이 필요하다.

### 출시 판단

- Reddit 제보만으로 앱 충돌·데이터 손실·보안·결제 장애 같은 긴급 핫픽스 사유는 확인되지 않았다. 별도의 즉시 재빌드보다는 예정된 1.6.0 preview APK 빌드와 최종 실기 검증 흐름을 유지한다.
- 다만 학습 앱에서 단어 난이도, 정의, sense와 예문의 의미 대응은 핵심 신뢰도 문제다. 기술적 치명도는 낮아도 콘텐츠 품질상 중요하며, 특히 현재 상태로 `Advanced Korean 500`이 `TOPIK II 5-6`을 강하게 표방하는 것은 재검토가 필요하다.
- 예문 속 단어/문법 탭 카드 생성, 다른 채소 캐릭터, 추가 표정은 즉시 구현 약속 없이 향후 아이디어로만 보관한다.

### Claude에서 이어갈 권장 순서

1. FAQ·약관의 구 정책 문구(`게스트 10단어`, 광고 1회 `+10` 등)를 ko/en/es 모두 새 Free 정책에 맞게 정리한다.
2. `Advanced Korean 500`의 출처·등급 의미를 다시 확인하고, 실제 TOPIK 5–6 덱이라고 부를 근거가 부족하면 제목/설명을 낮추거나 단어 목록을 재선정한다. 단순히 예문만 어렵게 만들어 표제어 난이도를 고급으로 포장하지 않는다.
3. 공식 한국어 덱 전체에서 다음을 기계적으로 탐지하고 사람이 표본 검수한다: `definition === meaningKr`, 예문에 표제어 또는 올바른 활용형이 없음, 여러 의미 대비 예문 부족, 초·중급 표제어의 고급 덱 혼입, sense-예문 의미 불일치.
4. preview APK를 빌드한 뒤 영어 온보딩 `Next`의 실제 렌더링과 유효한 BYOK 키를 사용한 AI 덱 생성을 실기 검증한다. 실패 시 정확한 오류 전문·HTTP 상태·기기/OS·키 경로(BYOK/Edge)를 기록하고 원인 확인 전 추측 수정하지 않는다.
5. 위 검증과 기존 핸드오프의 최종 실기 항목을 통과한 뒤 1.6.0 출시 여부를 결정한다.

## 2026-08-19 — 한국어 학습 사다리 4덱 재설계 (진행 중)

- 상태: 진행 중 · 브랜치 `feat/curation-meaning-lang` · 전부 미커밋
- 배경: 레딧 제보 ②(Advanced 덱에 초급 단어). 파 보니 같은 결함이 **7개 덱**에 있었다.

### 문제 덱 실측 — 원인은 `TARGET_COUNT = 500` 한 줄

| 덱 | 원본 | 채택 | 자른 축 | 담긴 것 |
|---|---|---|---|---|
| 고급/중급 중국어 (HSK 5·3급) | 1,074 / 976 | 500 | **병음 알파벳순** | `àn`~`jìnhuà` — **k~z 없음** |
| 고급/중급 일본어 (JLPT N1·N3) | 2,829 / 1,687 | 500 | 빈도 상위 | N1 첫 단어가 `私`(나) |
| Advanced/Intermediate/Basic Korean | 1,142 / 1,371 / 660 | 500 | 빈도 상위 | 그녀·정부·과정 |

원본 목록은 이미 난이도로 갈려 있는데 자를 때 쓴 축이 난이도와 무관했다. 중국어 두 덱은
난이도가 아니라 **커버리지 결함**이다. NGSL·BSL·NAWL·TSL 은 공인 목록 전량이라 무사하다.

### 결정 (은정님)

1. 이번엔 **한국어 3덱만**. 중국어·일본어는 다음 차례.
2. **자르지 않는다 + Intermediate 분할** — 후보 풀 3,168개를 4덱이 빈틈없이 나눠 갖는다.
3. 생성은 **기존 덱 파이프라인** 유지(형식이 기존 1,482장과 어긋나면 안 된다).

```
Basic Korean     665  A등급 전량            기존 486 + 신규 179
Intermediate I   786  B584 + C202           기존 700 + 신규  86
Intermediate II  786  B786                  기존   3 + 신규 783
Advanced Korean  931  C931 (rank>=1400)     기존 293 + 신규 638
```

C등급 202개(그녀·따라서·오히려)를 Intermediate 로 내려 **사라지는 어휘를 0으로** 만들었다.

### 이번 세션에 끝낸 것

- 표제어 선정 4덱 3,168개 — 중복 0 · 누락 0 · 테스트 19개(`__tests__/ko-ladder-split.test.ts`)
- 한국어 뜻풀이 캐시 3,165/3,168 (`seed-cache` 831건, 실제 ₩659)
- 시딩 오염 차단 — `official-deck-compose.ts` 가 캐시 최상위 definition 을 통째 복사하던 것을
  **덱 뜻과 겹치는 sense 만** 쓰도록 수정. definition 교정 4,907 → 4,434 (473건이 오염 위험군)
- 검수 도구 `scripts/list-mono-senses.ts` · 캐시 교정 `scripts/fix-mono-senses.ts`(dry-run 통과)

### 🔴 남은 일 (다음 세션)

1. **카드 생성 466/1,686 → 나머지 1,220개.** Gemini 무료 한도 소진(flash 18배치·lite 8배치에서
   429). 하루 ~450개라 사흘. `npx ts-node -P tsconfig.scripts.json scripts/translate-ko-ladder-vocab.ts`
   로 재개하면 progress 파일 덕에 467번째부터 이어진다.
2. **`fix-mono-senses.ts` 실행** — 단음절 캐시 32건이 오답이다(`급`=혈액형 `비`=blood `이`=잇몸
   `말`=동물의 울음소리). 병기는 카드 앞면까지 덮으므로 그대로 두면 오답을 외운다.
   dry-run 32/32 통과, **enrich_cache 32행 UPDATE 는 미실행**.
3. 통합(`integrate-ko-ladder.ts`) → `diagnose:decks` → 서버 재시딩
4. **신규 표제어 단음절 재검수** — 검수한 118건은 기존 덱 기준이라 `논`·`미`·`한`·`소`·`절` 등
   새 표제어는 빠졌다. 통합 후 `list-mono-senses.ts` 재실행.
5. 병기 순서 3건(`시`=詩인데 時가 앞, `예`, `일`) — 삭제가 아니라 순서 조정 건
6. 중국어 HSK 2덱 · 일본어 JLPT 2덱

### 알아 둘 것

- **덱은 서버 데이터라 1.6.0 빌드를 막지 않는다.** `curationData.ts` 수정 → 재시딩이면 반영된다
  (1.6.0 이상 앱 대상). 레딧 제보자가 본 것은 번들 덱이고, ④⑤ 는 서버에서 이미 고쳐져 있다.
- **표제어에 NIKL 동음이의어 번호가 붙어 온다**(`{{ko-linker|정성11|명}}`). 떼지 않으면 "정성11"
  이 카드에 찍힌다. 옛 스크립트는 빈도 상위만 써서 안 걸렸을 뿐이다.
- **옛 Basic·Intermediate 는 입력 파일 6개 중 3개만 읽었다**(rank ~1500). A등급 205개가 후보에
  오른 적이 없고, 모자란 자리를 B로 메우다 중복 18건이 났다.
- `constants/curationData.ts` 는 **CRLF**. LF 로 쓰면 8.7MB 전 줄이 바뀐 것으로 표시된다.
- 앱의 `canBlankExample` 은 `그러다`+`그렇게`를 통과시키고 `자르다`+`잘랐어요`를 탈락시킨다 —
  예문에 표제어가 쓰였는지 형태만으로 판정할 수 없다. 그래서 생성 때 모델이 쓴 활용형을
  `usedForm` 으로 받아 대조한다.

### 2026-08-19 이어서 — 커밋 · 캐시 2차 교정 · 한도 실측

- 커밋 3개(`6334f1b` 시딩 오염 차단 · `5e3299a` 사다리 파이프라인 · `3e51dd1` 캐시 2차 교정).
  미커밋 잔여는 `scripts/ko-ladder-translated.json` 하나뿐이다 — **25개짜리 옛 시험 산출물**이라
  일부러 뺐다. 🔴 `integrate-ko-ladder.ts:154` 가 이 파일을 읽으므로, 지금 통합을 돌리면 조용히
  25장짜리 덱이 된다. 본 출력은 생성이 **전부** 끝나야 써진다.
- 🔴 `.ko-ladder-progress.json` 을 gitignore 예외로 두고 커밋했다. 원래 `scripts/.*-progress.json`
  (기존 규칙 29행)에 걸려 있었는데, 이 파일만은 "재생성 가능" 이 아니다 — 생성한 카드가 들어 있고
  다시 만들려면 사흘이다. 워킹 트리를 여러 세션이 공유하므로 추적 밖에 두면 유실 위험이다.

#### 🔴 무료 한도 실측 — 모델당 하루 20 요청

429 본문에서 확정: `quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`,
`quotaValue: 20`, model 단위. 배치 25개이므로 **모델 하나로 하루 500단어**가 상한이다.
flash 와 flash-lite 는 버킷이 따로라 합쳐 하루 1,000단어. 남은 1,095개면 이틀이다.

🔴 **초기화는 KST 자정이 아니라 PT 자정 = KST 16:00 이다.** 그리고 **1토큰 탐침으로는
한도를 못 잰다** — maxOutputTokens=1 요청은 200 을 받았는데 실제 배치는 429 였다.
남아 있던 마지막 요청 몇 개를 탐침이 써 버린 것이었다. 재려면 진짜 배치 프롬프트로 재라
(`buildPrompt` 를 그대로 불러 한 번 쏘고 본문을 읽는다).

#### 캐시 2차 교정 20건 (적용 완료)

새 4덱 단음절 218개 중 병기가 실제로 붙는 112건을 전수로 읽고, 그 표제어의 뜻이 아닌
20건을 지웠다. 8건은 뜻이 하나만 남아 병기가 해제됐다. 적용 뒤 병기 대상 112 → 104.

- 🔑 **검수 대상은 senses 2개 이상 전부가 아니다.** 캐시 뜻이 덱 뜻과 하나도 안 겹치면
  병기가 통째로 건너뛰어져(`senses-skipped-nooverlap`) 오답이 카드에 안 실린다. 실측
  senses>=2 는 177건인데 실제 병기는 104건. `미`(캐시 3개가 전부 오답)가 이 경우다.
- 🔑 **카드 생성을 기다릴 필요가 없었다** — 단음절 218개 중 172개는 기존 덱 단어라 이미
  뜻이 있었다. 남은 46개만 카드 대기.
- 🔴 `list-mono-senses.ts` 는 이 검수를 못 한다. `curationData.ts` 의 단어만 돌기 때문에
  통합 전에는 사다리 표제어가 전부 빠진다. `list-mono-merged.ts` 를 새로 뒀다.
- 🔴 그 파일의 `deckMeaningLost` 는 쓰지 말 것. 덱 뜻을 쉼표로 쪼개 문자열 포함으로 재서
  `soup (served with rice, thinner than stew)` 가 괄호 안에서 갈리고 `what (informal)` ↔
  `What (colloquial)` 같은 바꿔 쓰기까지 "잃음"으로 잡는다. **51건 중 진짜는 8건이었다.**

#### 미결로 남긴 것 (은정님 판단으로 이번엔 A만 반영)

- **B. 병기가 덱 뜻을 지운다 8건** — 캐시가 그 동음이의어를 아예 모르면, 병기가 덱 뜻을
  자기 뜻들로 갈아치운다. 삭제로는 못 고치고 재생성이 필요하다.
  `위`(胃 소실) `극`(drama) `성`(castle) `종`(slave) `골`(goal) `전`(全) `열`(列) `불`(light)
- **C. 순서 3건** — `시`(詩인데 時가 ①②) `예`(例인데 대답 '예'가 ①) `일`(work인데 ①이 "하루의 기간")
- **D.** `색` ③ "남녀 간의 애정이나 성적인 관계" — 색(色)의 실제 뜻은 맞지만 **basic 덱의
  `color` 카드 앞면**에 병기로 실린다.

#### ✅ 캐시 오염 청소 완료 — 78건 → 0건 (2026-08-19)

🔴 **처음 보고한 "65행" 은 틀린 수였다.** 전수 스캔이 `range()` 페이지네이션을 **정렬 없이**
썼다 — 이 저장소가 이미 당한 함정이고 `seed-cache.ts:434` 에 "75,010행 중 46,779행만 읽어
재호출한 사고" 라고 적혀 있는 바로 그것이다. 중복으로 부푼 동시에 놓친 행이 있었다.
정렬(`source_lang,target_lang,term,prompt_version`)을 넣고 다시 읽으니 **78행**이었다.

| 단계 | 결과 |
|---|---|
| 전수 재검사 | 78행 (v7 77 · v6 1) · hit_count 전부 0 |
| 1차 재생성 75건 | 전부 되살아남 · **12건이 다시 오염(16%)** |
| 게이트 수정 후 2차 12건 | 게이트가 잡아 자동 재시도 → 6건 회복 |
| 손으로 고침 6건 | 재생성으로는 끝내 안 나은 것 |
| **최종 전수 재검사** | **오염 0** (`thomas` 1건은 오탐) |

비용 약 ₩72(₩0.75/건). 🔑 **시딩은 유료 Vertex 라 카드 생성의 무료 한도와 지갑이 다르다** —
같은 날 동시에 돌려도 서로 안 뺏는다.

##### 🔴 근본 원인 — 게이트가 이 결함을 원래부터 못 잡았다 (`6d6df37` 로 수정)

① `scriptViolation` 은 한글·가나·한자 셋만 안다. "**기대한** 문자가 충분한가" 만 보므로
   키릴·아랍·벵골·데바나가리·히브리·타이는 셈에 **아예 안 들어간다**.
   `en>ko renowned`("널리 알려진; مشهور한")는 한글 비율 0.67 로 통과했다.
② `defectOf` 가 **`definition` 을 아예 안 봤다**(meaningKr·exampleEn·exampleKr 뿐).
   오염이 definition 에만 있는 `en>ja shipping` 은 그냥 통과했다.

→ `alienScript` 신설. `definition` 에는 **지배 비율 검사를 걸지 않는다** — definition 은
출발어 단일어 뜻풀이라(en>ko 의 definition 은 영어) 걸면 정상까지 재생성 사유가 된다.

##### 다음에 이 작업을 또 할 때 알아야 할 것

- 🔴 **재생성만으로는 16% 가 같은 자리에 또 오염된다.** 게이트 수정 후에도 재시도 7건 중
  1건만 회복됐다 — 특정 표제어(`shipping`·`headquarters`·`renowned`·`diplomacy`·`好奇`)는
  모델이 고집스럽게 같은 결함을 낸다. **손으로 고치는 단계를 예산에 넣을 것.**
- 🔴 **오염은 치환이 대부분이지만 삽입도 있다.** `zh>vi gkd` 는 "赶快**Да**，" 로 끼어든
  것이라 지우면 멀쩡해졌다. 반면 `ko>ja 밤` "해시 **от** 해 뜰 때까지"(← 해질 때부터)는
  지우면 구멍이 남는다. **한 건씩 보고 갈라야 한다.**
- 🔴 **오탐 세 종류를 빼고 세라**: `phonetic` 1,603건은 **IPA**(`ˈdjeθ` 의 θ, `ˈnweβe` 의 β),
  그리스 27행은 `alpha`·`beta`·`pi`·`파이` 등 **표제어 자체**, `en>ko thomas` 는 아람어
  어원을 **일부러 인용**한다(어원 인용과 오염은 형태로 구별되지 않아 규칙에서 빼지 않았다).
- `en>ja headquarters` 는 키릴 말고 결함이 하나 더 있었다 — **예문번역이 예문을 옮기지 않고**
  뉴욕을 스톡홀름으로 바꾼 뜻풀이 문장이었다. 오염을 보러 갔다가 다른 결함을 만날 수 있다.
- v6 354행 · v5 4행이 남아 있다. 앱은 v7만 조회하므로 무해하지만 스캔에 잡힌다
  (v6 `en>zh transient` 의 오염 필드는 **v7에서 없앤 `mnemonic`** 이라 검사 대상도 아니다).
- 이 과정에서 `fix-mono-senses.ts` 의 `FOREIGN_SCRIPT` 에 `g` 가 없어 `replace` 가 첫 글자
  하나만 지우고 있던 것도 고쳤다. `정`의 벵골 문자 4자 중 1자만 지워져 있었다.

### 2026-08-19 이어서 (2) — 유료 전환 · 카드 완주 · 모델 일원화

#### ✅ 카드 1,686장 완료 (커밋 `e3ca880`)

무료 한도(모델당 하루 20요청)로 사흘 걸릴 일이었다. 병목은 모델 속도가 아니라 **요청 수**였고,
그 20요청 중 **40%가량을 자투리 예문 재생성이 먹고 있었다** — 1~3개짜리 재시도가 25개짜리
배치와 같은 값 1요청을 치른다. 무료로 되돌아갈 일이 있으면 이것부터 모을 것.

| 검증 | 결과 |
|---|---|
| 총 카드 | 1,686 · 표제어 중복 0 · 빈 필드 0 |
| 예문 길이(어절, 신규/레거시) | basic 3.7/3.8 · inter1 7.3/7.3 · inter2 7.5/7.3 · advanced 11.3/11.1 |
| `usedForm` 실패 | 0 |

`알` 하나만 손으로 고쳤다(예문이 표제어 대신 "달걀"을 썼다 — 식용 맥락에서 한국어는 `알` 보다
"달걀"을 쓰므로 모델이 계속 샌다). `골짜기` 미통과는 오탐 — `산골짜기` 가 표제어를 온전히 품는데
`exampleUsesTerm` ②의 초성 검사가 첫 글자만 본다.

#### 🔴 유료 전환 — `.env` 키가 바뀌었다

`avocado-491710`(Vertex/Edge 프로젝트)에서 발급한 **신형 auth key**(`AQ.` 로 시작, 53자).
결제를 새로 튼 게 아니라 이미 결제가 붙은 프로젝트에서 키를 만든 것이다. 다만 그 프로젝트가
**"Tier 1 · 선불"인데 크레딧 0** 이라 $10 충전이 필요했다 — 선불 잔액이 0이면 무료 티어로
폴백하지 않고 그냥 죽는다. Vertex(후불)와 **지갑이 다르다**.

#### 🔴 gemini-2.5 는 신규 프로젝트에서 이미 못 쓴다 (커밋 `0a9a90f`)

"10/16 은 하한선이라 여유 있다"는 기록이 뒤집혔다. 새 키를 만든 순간 404 다
(`no longer available to new users`). 기존 키만 살아 있어 **날짜만 보고 있으면 못 본다.**

- 🔴 **모델 목록 API 에는 2.5 가 그대로 보인다** — 판정은 실제 `generateContent` 호출로.
- `scripts/` **37개가 한꺼번에 죽어** `scripts/_shared/model.ts`(+ESM 용 `.mjs`)로 모았고,
  동기화 테스트 범위에 `scripts/` 를 넣었다. 그 테스트는 원래 "scripts/ 는 사용자에게 안 닿는다"며
  일부러 빼 놓았던 것이다 — **안 닿는 것과 안 죽는 것은 다르다.**
- 🔑 테스트가 **grep 이 놓친 파일**을 잡았다: `generate-curation-data.ts` 만 `gemini-1.5-flash` 를
  `v1` 로 불러 `gemini-2.5` 검색에 안 걸렸다. 1.5 은퇴 이후 줄곧 죽어 있었을 것이다.
- ✅ 앱(`lib/ai/model.ts` = 3.5-flash-lite)과 Edge(Vertex)는 무사하다.

#### 🔴 예문 길이 스펙은 처음부터 지켜진 적이 없었다

`LEVEL_SPEC.words` 옛 값이 레거시 실측과 어긋나 있었는데도 문제가 없었던 것은 **옛 덱도
2.5-flash 로 만들어서** 같은 모델이 같은 길이를 냈기 때문이다. 일관성을 지탱한 것은 스펙이
아니라 모델이었다. 3.5-flash-lite 는 스펙을 실제로 따르므로 inter2 가 7.1 → 9.4어절로 튀었다.

- 🔑 보정식: 이 모델은 **지정 범위의 중앙값보다 1.2~1.7어절 짧게** 낸다(관측 3회).
- ⚠️ `basic` 만 실측 못 했다(생성할 표제어가 없었다). 다시 만들면 먼저 잴 것.
- 🔴 **모델을 바꿀 때는 한 배치로 길이부터 재라.**

#### 🔴 병기가 뜻 3줄을 넘겨 잘린다 — 사다리와 무관한 기존 문제 (미해결)

`official_words` 실측: 시딩된 13,874장 중 **48%가 병기 상태**, ko>en 3덱은 병기 790장 중
**229장(29%)이 3줄 초과 추정**, 최대 **236자**(`아`). 플래시카드 뒷면에서 예문은 자기
`ScrollView` 안에서 스크롤되지만 **뜻은 `numberOfLines={3}` 으로 잘린다** → ②③ 소실.
**예문 길이는 이 문제와 무관하다**(별도 영역).

- 🔴 **"3줄 = 75~87자"는 폰트 메트릭 계산치다. 실기로 재기 전에 고치지 말 것.**
  재는 법: `아`(236자) 카드를 열어 몇 번째 글자에서 잘리는지 본다. 기기 하나로는 부족.
- 🔑 길이보다 나쁜 것은 **중복 뜻**이다 — `사업` ①②③이 전부 "Business", `오빠` ②③도 사실상 같다.
- 🔴 **문자열로 중복을 판정하지 말 것** — `deckMeaningLost` 가 같은 접근으로 51건 중 8건만 맞혔다.
- `SAVE_LIMITS.meaningKr = 300` 은 저장 한도이지 **화면 한도가 아니다**.

### 2026-08-19 이어서 (3) — 통합 완료. 통합은 깨끗했고, 문제는 그 앞 단계에 있었다

#### ✅ 4덱 통합 3,168장 (커밋 `b391020`)

`Basic 665 · Inter I 786 · Inter II 786 · Advanced 931` = 재사용 1,482 + 신규 1,686.
표제어 중복 0 · 빈 필드 0 · 어휘 손실 0. **덱 단위로 비교해 나머지 61덱은 내용 무변경**을
확인했다(diff 줄 수 6만은 카드 증가 + 들여쓰기 정규화 36곳이다 — 줄 수로 놀라지 말 것).

#### 🔴 신규 카드 로마자 138건(8.2%)이 틀려 있었다 — 레거시는 0건

**`translate-ko-ladder-vocab.ts` 가 `checkRomaja` 를 부르지 않았다.** 사다리 전용 스크립트를
새로 만들면서 빠졌고 예문 검사만 있었다. → 게이트를 넣었다(✅). **재생성이 아니라 그 자리에서
규칙으로 고정**한다 — 오류가 무작위라 재생성으로는 낫지 않고, 요청도 아낀다.

| 유형 | 건수 | 예 |
|---|---|---|
| 표기법 | 127 | 비음화 `음료` eumryo · 유음화 `분리` bunri · 연음 `활용` hwalyong |
| 한글 잔존 | 7 | `위협` → "위협" · `가라앉다` → "gar앉da" |
| 이질 문자 | 2 | `분노` → "bunn**ೋ**"(칸나다) · `제의` → "j**ē**i" |

🔴 첫 오염 검사가 한자·가나·키릴·아랍만 봐서 **칸나다를 놓쳤다**. 로마자 필드는 문자 집합을
열거하지 말고 **ASCII 소문자 외 전부**로 볼 것.

#### 🔴 AI 가 맞고 변환기가 틀린 자리가 2종 — 일괄 교체했으면 정답을 오답으로 바꿨다

선례 E(ㄴ첨가 `알약` allyak)와 같은 함정이 또 나왔다. 그래서 새 `scripts/fix-deck-romaja.ts`
는 **후보 어디에도 맞지 않을 때만** 바꾼다.

- **구개음화** `묻히다`[무치다]·`갇히다`[가치다] — 표준 발음법 제17항 붙임이고, 로마자 표기법
  제3장 제1항 3호의 예시가 `굳히다 guchida` 다. `romanize.ts` 의 구개음화 분기가 **뒤 초성이
  'ㅇ'인 경우(같이·굳이)만** 봐서, 격음화로 'ㅌ'가 된 뒤 멈추고 있었다.
- **겹받침 격음화** `넓히다` — AI(neolhpida)도 변환기(neolhida)도 **둘 다 틀렸다**(정답
  neolpida). 표준 발음법 제12항이 ㄱ(ㄺ)·ㄷ·ㅂ(ㄼ)·ㅈ(ㄵ)을 명시하는데 `jongAsCho` 에 ㄺ만
  있었다. 겹받침은 **대표음이 아니라 뒤 자음**이 ㅎ과 합친다.

#### 🔴 어제 커밋이 사다리 테스트를 꺼뜨리고 있었다

`0a9a90f` 가 `scripts/_shared/model.mjs` 를 만들었는데 `transform` 은 `.ts` 만 변환하고
preset 기본 해석 순서는 **mjs 가 ts 보다 앞**이다. `ko-ladder-split.test.ts` 가 SyntaxError 로
죽어 있었다 — 하필 이번에 통합한 `splitLadder`·`fixRomajaAspiration` 을 고정하는 테스트다.
`jest.config.js` 에 `moduleFileExtensions` 를 명시해 되살렸다(19 test 복귀).
🔑 **suite 가 죽으면 test 수만 보고는 모른다.**

#### 🔑 `curationData.ts` 가 CRLF 라는 기록은 틀렸다

`core.autocrlf=true` 라 **blob 은 전부 LF**, 워킹트리는 CRLF 231,251줄 + LF 5,716줄 혼재였다.
어느 개행으로 쓰든 diff 에는 내용 변경만 잡힌다.

#### 검증 · 손수정

- `diagnose:decks` — ko 4덱 확실한 문제 **141 → 1건**(남은 1건 = `문`/`닫다` 예문 중복,
  **그대로 두기로 함**). 기존 61덱 18건 그대로 = 회귀 0.
- `npx jest` — **1,095 test 전부 통과**. 실패 3 suite 는 기존 환경 문제(sqlite3 미설치 2,
  photo-import 파서 1).
- 손수정 3건: `후보` meaningKr 의 한자(候補) · `맞이하다` 영어 번역 복구(usedForm 이 번역
  칸에 들어가 있었다) · `의자` 예문 교체.
- 🔑 **로마자 빈칸 134건**(krteen·krslang 등)은 별건이라 건드리지 않았다. 채우려면 먼저
  변환기가 **여러 어절을 이어 붙여 공백을 잃는 것**부터 고쳐야 한다(`폼 미쳤다` →
  `pommichyeotda`).

#### ✅ 서버 시딩 + 공개 완료 (같은 날, 커밋 없음 — 서버 상태 변경이다)

```bash
npx -y tsx scripts/seed-official-decks.ts --deck curated-ko-basic-1 --deck curated-ko-intermediate-1 \
  --deck curated-ko-intermediate-2 --deck curated-ko-advanced-1
npx -y tsx scripts/seed-official-decks.ts --publish --deck curated-ko-intermediate-2
```

| 합성 결과 | 수 |
|---|---|
| 대상 | 4덱 3,168장 · 캐시 미스 3(`가하다`·`기초하다`·`볼` — 알려진 것) |
| definition 손봄 | **3,084 (97%)** — 빈칸 채움 1,130 + 복사본 교정 723 + 병기가 덮는 나머지 |
| ①② 병기 적용 | **1,231 (39%)** — 신규 카드 505 · 레거시 726 |
| 예문이 캐시 것으로 바뀜 | 1,231 (병기와 같은 수) |
| 병기 보류(덱 뜻이 캐시에 없음) | 81 |

서버 실측(anon 키로 확인 — service_role 로는 RLS 를 안 지나 의미가 없다):
`basic 665 · inter I 786 · inter II 786 · advanced 931`, 네 덱 모두 `is_published=true`.
definition 이 실제로 한국어 뜻풀이가 됐다(`의자` → "사람이 앉을 수 있도록 만든 물건.").
로마자 교정도 서버까지 갔다(`분노 bunno` · `위협 wihyeop` · `묻히다 muchida`).

##### 🔴 정정 — "기존 3덱은 즉시 사용자에게 나간다"는 틀렸다

시딩 직전에 그렇게 적었는데, **서버 플래그만 보고 앱 배포 상태를 안 본 것**이었다.
서버 덱을 읽는 코드(`2acf185`·`ec2c791`, 8/18)는 **`main` 에 없다** — 스토어 앱에는 그 기능이
아예 없고 여전히 번들 안 `curationData.ts` 를 읽는다. 그래서 **이번 시딩과 공개는 아직 아무에게도
도달하지 않았다.** 실질 공개 시점은 **1.6.0 이 스토어에 나가는 순간**이고, 그때 published 인 덱이
한꺼번에 보인다.

🔑 **"서버가 공개 상태다"와 "사용자가 본다"는 다른 문제다.** 판정하려면 RLS(`is_published`,
anon 읽기 허용)와 **그 기능이 들어간 커밋이 출시 브랜치에 있는지**를 둘 다 봐야 한다.
→ `git merge-base --is-ancestor <커밋> origin/main`

##### 🔑 병기 대상 카드에 손수정을 해도 시딩이 덮는다

통합 때 손으로 고친 3건 중 2건이 병기로 덮였다.

- `의자` — senses 0 이라 **내 예문이 그대로 남았다** ✅
- `후보` — 병기로 덮였으나 한자(候補)는 캐시 뜻으로 교체되며 **사라졌다** ✅
- `맞이하다` — 병기로 덮였다. 손으로 쓴 영어 번역은 안 쓰였고 캐시 ①② 가 들어갔다.
  결함(usedForm 이 번역 칸에 있던 것)은 해소됐다.

**손수정 전에 그 표제어의 캐시 senses 가 2개 이상인지 볼 것** — 그러면 헛일이 된다.

##### 🔴 뜻 3줄 잘림이 서버에 231장 (7.3%) 올라가 있다

`meaning_kr` 87자 초과 기준. 최장 `아` 236자. 병기 1,231건 중 19%다. 이건 **예상하고 올린
것**이지 사고가 아니다 — 다만 실기 측정 전에는 손대지 않기로 한 항목이므로, 다음 빌드에서
**몇 번째 글자에서 잘리는지부터 재고** 고칠 것. 87자는 계산치다.

🔑 **뜻이 전부 같은 되풀이(`시월` = October/October/October)는 7건(1%)뿐이다.** `시월` 하나를
보고 "중복 뜻이 문제"로 확대할 뻔했다. 완전 일치로만 세면 오탐 없는 하한이 나온다(부분 일치·
쉼표 분해로 재면 51건 중 8건만 맞히던 그 실패로 돌아간다).

### 다음 (우선순위 순)

1. ~~**통합 → 검수 → 시딩 → 공개**~~ ✅ 전부 완료. **다음은 개발 빌드 실기** — 새 덱
   `Intermediate Korean II` 가 큐레이션 탭에 뜨는지, 786장이 다 로드되는지.
2. **뜻 3줄 잘림 231장** — 실기로 몇 자에서 잘리는지 재는 것이 선결. 그 전에는 손대지 않는다.
1. ~~**통합 → 검수 → 시딩.**~~ 🔴 통합은 `curationData.ts`(8.7MB·CRLF)를 다시 쓰므로 실행 전 확인받을 것.

   ```bash
   npx ts-node -P tsconfig.scripts.json scripts/integrate-ko-ladder.ts --dry-run
   npx ts-node -P tsconfig.scripts.json scripts/integrate-ko-ladder.ts
   pnpm run diagnose:decks && npx jest
   ```
2. 재시딩 뒤 `list-mono-merged.ts` 재실행 — 남은 46개 표제어 검수.
3. 미결 B·C·D. (캐시 오염은 ✅ 완료)
4. **병기 3줄 문제** — 다음 빌드에서 실기 측정부터. 그 값이 나오기 전에는 손대지 않는다.
5. 중국어 HSK 2덱 · 일본어 JLPT 2덱. 🔴 그 스크립트들도 이제 새 키로 돌지만,
   **덱마다 레거시 예문 길이를 먼저 재야 한다** — 아직 아무도 안 쟀다.

---

### (옛 계획 — 무료 한도 시절. 위 항목으로 대체됨)

1. ~~**KST 16:00 이후 카드 생성 재개 — 먼저 `--batch=50` 을 한 배치만 시험한다(은정님 결정).**~~

   ```bash
   # ① 시험: 50개 한 요청 (하루 20요청 중 1개를 쓴다)
   npx ts-node -P tsconfig.scripts.json scripts/translate-ko-ladder-vocab.ts --batch=50 --limit=50
   # ② 대조: 돌아온 50장을 기존 25짜리와 비교 — 개수 50인가 · 예문 길이 · usedForm 검사 통과율
   # ③ 멀쩡하면 --batch=50 으로 계속, 아니면 옵션 없이(=25) 진행
   npx ts-node -P tsconfig.scripts.json scripts/translate-ko-ladder-vocab.ts --batch=50
   npx ts-node -P tsconfig.scripts.json scripts/translate-ko-ladder-vocab.ts --batch=50 --model=lite
   ```

   🔑 한도는 **요청 수**로 걸리므로 하루 산출 = `20 × batch`. 25면 두 모델 합쳐 1,000단어라
   이틀, 50이면 2,000단어라 **하루에 끝난다**. 프롬프트 문구는 그대로라 카드 형식은 안 바뀐다.
   짧게 와도 유실은 없다 — 받은 것만 저장하고 빠진 표제어는 다음 실행에 다시 잡힌다.
2. 통합 전 `scripts/ko-ladder-translated.json` 이 1,686개인지 확인(지금 25개).
3. 통합 → `pnpm run diagnose:decks` → `npx jest` → 서버 재시딩.
4. 재시딩 뒤 `list-mono-merged.ts` 재실행 — 남은 46개 표제어 검수.
5. 미결 B·C·D. (캐시 오염은 ✅ 완료)

## 2026-08-20~21 — 덱 definition 결함 정리 (완료) · 다음은 1.6.0 제출

- 상태: 완료 · 브랜치 `feat/curation-meaning-lang` · 커밋 3개 전부 푸시됨
- 커밋: `c84d0a7` CJK 구분자 · `8d6b83a` 사람 판정 전 덱 확대 · `3a8b3e9` 수능 3장 비움

### 결과 (서버 실측, 65덱 15,542장)

| | 시작 | 지금 |
|---|---|---|
| definition == meaning_kr 복사본 | 344 (2.2%) | **200 (1.3%)** |
| 빈 definition | 75 | 63 |
| **실사용 덱(도착어 ko·en) 복사본** | 68 | **0** |

남은 복사본 200장은 전부 도착어가 ja/zh/vi 인 덱이다. `cloud_lists` 실측상
**ko>ja·ko>zh·ko>vi 단어장은 0건**(en>ko 104개/43명 · ko>en 37개/23명)이라
지금 이걸 더 고쳐도 보는 사람이 없다.

### 이번에 고친 것

1. **CJK 구분자** — `normMeaning` 이 지우는 구두점에 `、，。；：！？` 가 없어, 겹침 판정이
   일본어·중국어 낱말 경계를 못 봤다. `こと、もの` 가 통째로 한 토큰이 됐다.
   🔴 전각 괄호 `（）` 는 지우면 안 된다 — CJK 는 괄호 안이 뜻 자체라 `数詞（一つ）` 에서
   떼면 겹침이 사라진다. 시뮬레이션에서 멀쩡하던 판정 4건이 그렇게 깨졌다.
2. **사람 판정을 전 덱으로** — `scripts/lib/definition-decisions.ts`(이름에서 ko-ladder 뺌).
   실사용 덱 결함 100장을 전량 읽고 fill 119 / blank 63 으로 갈랐다.
   🔑 같은 "겹침 실패"인데 덱마다 처방이 반대다: 수능은 캐시가 그 단어의 다의어라 **채우고**,
   사극은 캐시가 다른 한자어(신=神, 짐=화물, 기생=寄生)라 **비운다**.
3. **수능 3장(`reverse`·`bar`·`object`)** — 품사가 어긋나 덱 뜻이 정의에 아예 없었다
   (bar 덱="막다"(동사) ↔ 캐시는 막대기/바 카운터/술집). 비웠다.

### 다음 세션 순서

📋 **열린 버그·미결은 `docs/bugs-open.md` 한 곳에 모았다**(2026-08-21). 빌드 전에 고칠
것(A) · 그 빌드에서만 갈리는 것(B) · 미뤄도 되는 것(C) · 데이터/서버(D)로 갈라 뒀고,
각 항목에 근거와 "오늘 확인함/이전 기록" 표시를 붙였다.

**① 1.6.0 제출이 먼저다.** 지금까지 고친 서버 데이터 전부가 출시 순간에야 사람에게 닿는다
(읽는 코드가 `main` 에 없다). 절차는 `docs/release-checklist-next.md`.
남은 것: 날짜 2곳(약관 lastUpdated · 소식 date) → 빌드 → **Play Console 등록정보 붙여넣기
(검토 들어가면 리셋되므로 제출 전에)** → 제출.

**② 그 다음 HSK 2덱 재생성.** 은정님 결정 — 표제어 선정부터 다시 해서 **전량 담는다.**

원인은 `scripts/build-zh-hsk-source.ts:33` 의 `TARGET_COUNT = 500` 이다. HSK 위키 페이지가
빈도순이 아니라 **음절 그룹 + 병음 순**이라, 앞에서 500을 자르면 단음절 전량 + 2음절 a~j 가
된다. 🔴 "k~z 가 통째로 없다"는 기존 기록은 **틀렸다** — k 이후가 중급 115장(23%) ·
고급 112장(22%) 있다. 앞뒤 4장만 보고 판단한 결과였다.

| | 원본 파싱 | 중복 제거 | 현재 | 빠진 것 |
|---|---|---|---|---|
| HSK 3급 (`curated-zh-intermediate-1`) | 973 | 969 | 500 | **469** |
| HSK 5급 (`curated-zh-advanced-1`) | 1,071 | 1,070 | 500 | **570** |

🔑 500 으로 자를 **좋은 축이 없다** — HSK 페이지는 빈도순이 아니고 CC-CEDICT 에도 빈도가
없다. 그래서 사다리 4덱과 같은 결론(자르지 않는다)에 이르렀다.

실행 경로 (전부 남아 있고 재현 확인함 — 다시 돌려도 결과 파일 diff 0):

```
1) TARGET_COUNT 를 969 / 1070 으로 (또는 인자화)
   npx -y tsx scripts/build-zh-hsk-source.ts 3      → scripts/zh-intermediate-source.json
   npx -y tsx scripts/build-zh-hsk-source.ts 5      → scripts/zh-advanced-source.json
2) npx -y tsx scripts/translate-zh-intermediate-vocab.ts   (batch 25 · 재개는 progress 파일)
   npx -y tsx scripts/translate-zh-advanced-vocab.ts
3) npx -y tsx scripts/integrate-vocab.ts zh-intermediate   → constants/curationData.ts
4) pnpm run diagnose:decks → jest
5) npx -y tsx scripts/seed-cache.ts --pairs "zh>ko"        (신규 표제어 캐시)
6) npx -y tsx scripts/seed-official-decks.ts --deck curated-zh-intermediate-1 --deck curated-zh-advanced-1
```

- 신규 카드 **1,039장** · 예상 15분 안팎 · 비용 ~₩1,000(생성 수백 + 시딩 1,039×₩0.75).
- 🔴 `scripts/.zh-*-progress.json` 은 `.gitignore:29` 에 걸린다. 생성한 카드가 들어 있어
  재생성에 시간이 드니, 사다리 때처럼 **부정 규칙(`!`)으로 예외**를 두고 커밋할 것
  (`.gitignore:104-107` 이 선례).
- 🔴 **모델이 바뀌었으면 첫 배치로 예문 길이부터 재라.** 3.5-flash-lite 는 지정 범위 중앙값보다
  1.2~1.7어절 짧게 낸다. `LEVEL_SPEC` 을 그만큼 올려 잡아야 기존 카드와 길이가 맞는다.
- ⚠️ `背(bēi)/背(bèi)` 같은 다음자(多音字) 중복은 **결함이 아니다.** 서버에 덱당 3건 있다.
- 일본어 JLPT 2덱도 같은 결함이지만 원본이 N1 2,829 / N3 1,687 이라 신규 3,516장 —
  별도 예산 건으로 본다.

### 남겨 둔 것 (급하지 않음)

- 복사본 200장(도착어 ja/zh/vi) — 캐시 뜻이 번역어가 아니라 **정의문**이라 문자열로는 못 고친다.
  값싸게 끝내려면 `definition == meaningKr` 이면 비우는 규칙 한 줄이면 된다(정보 손실 0).
- 정의 첫 줄이 덱 뜻이 아닌 ~12장, 무관한 뜻이 뒤에 붙은 ~12장 — 첫 줄은 정확하거나
  정의 안에 답이 있다. 고치려면 애초에 실패한 sense 선택이 다시 필요하다.
- ~~**B·C·D 9건**~~ ✅ **2026-08-26 완료**(`182ebe7`). 원인은 덱 데이터가 아니라 **시딩 규칙**
  이었다 — 병기가 덱의 `meaningKr` 을 캐시 병기본으로 통째 교체했고, 겹침 판정은 뜻이 하나만
  겹쳐도 통과하므로 나머지 덱 뜻이 사라졌다.
  🔴 **9건은 눈으로 본 표본이었다 — 실제로는 63덱 5,256행**이고 vi·ja·zh 판에도 같은 결함이
  있었다(`위` 는 네 언어판 모두). 새 규칙은 **① = 덱이 가르치는 뜻 그대로, ②③ = 캐시가 아는
  다른 뜻**이고 예문도 ① 은 덱 것을 쓴다. 65덱 재시딩까지 끝(제어문자 0 · 공개 65 유지).
  `색`③ 만 남았는데 그건 순서가 아니라 부적절한 뜻이라 `sense-drops` 트랙이다.
- 판정 요청용 산출물 두 개는 소진됐고, 완료 표시를 붙여 `docs/` 로 옮겨 커밋했다
  (`docs/ko-ladder-fill-check.md` · `docs/ko-ladder-sense-review.md`, 2026-08-21).
  어떤 근거로 갈랐는지 되짚을 자리로 남긴 것이고, 판정 자체는
  `scripts/lib/definition-decisions.ts` 에 산다.

---

## 2026-08-25 — HSK 2덱 전량 재생성 (완료) · 다음 세션 착수점

- 상태: **완료**. 커밋 `66482f0`(9파일) · 서버 반영·공개까지 끝.
- 변경: 3급 500→**969** · 5급 500→**1070**. 제목도 실장수로 바꿨다
  (`중급 중국어 969 (HSK 3급)` · `고급 중국어 1070 (HSK 5급)` — `수능·학문 핵심 957` 선례).
- 검증: `official_themes`/`official_words` 실측 969·1070 · position 0~n 연속 · 뜻/예문 빈칸 0 ·
  공개 상태 유지 · `diagnose:decks` 신규 두 덱 확실한 문제 0건 · jest 1,193 통과(회귀 0).
- 비용: **₩1,110** (번역 ~₩250 + 시딩 ₩861). 예상 ₩1,000 대비 +11%.

### 이번에 새로 만든 것 — JLPT 2덱에 그대로 쓴다

| | 무엇 |
|---|---|
| `translate-zh-*.ts --repair` | 결함 항목만 다시 뽑는다. 결함 0.3% 에 전량 재실행은 낭비고 **새 결함을 만든다**(`保护` 가 재생성에서 또 영어를 넣었다) |
| `translate-zh-*.ts --terms=a,b,c` | 규칙이 아니라 **사람 판정**으로 고칠 자리("당신" 16건이 이 경로) |
| `integrate-vocab.ts --replace` | 기존 덱 교체(원래 "이미 존재합니다"로 막혀 있었다). 8MB 한 줄이라 정규식 말고 **중괄호 균형**으로 경계를 뜬다 |
| `build-zh-hsk-source.ts` | 기본값이 **전량**이 됐다. 줄이려면 두 번째 인자 |

🔑 셋 다 **별도 스크립트가 아니라 같은 파일에** 넣었다 — 프롬프트를 복제하면 조용히 어긋난다.

### 🔴 다음에 반복될 함정 셋

1. **첫 배치 25장은 표본이 아니다.** `--limit=25` 값이 전량과 전부 달랐다 — 예문 평균
   12.5 vs **14.0자**, 길이 준수 100% vs **88%**, 라틴 혼입 0 vs **3건**. rank 순서가
   음절 그룹 순이라 앞 25개가 전부 단음절 기본어였기 때문이다(표제어 1자 13.5 → 4자+ 16.5).
   **시험 배치는 뿌려서 뜨거나, 전량 뒤 반드시 다시 재라.**
2. **검사기가 「있어야 할 문자」만 보면 샌다.** "한자가 있고 한글이 없으면 통과"로 짜서
   `我们要保护 Environment.`, `很多 traditional 的地方美食`, `被纸kè伤了`(병음) 3건이
   전부 빠져나갔다. **있으면 안 되는 문자도 볼 것.** 파싱은 성공하므로 JSON 검사로는 못 잡는다.
   ⚠️ `exampleKr` 에는 라틴을 걸지 말 것(QR코드·TV 는 정상). ⚠️ `meaningKr` 은 규칙으로
   못 거른다 — `감정,的情感`(결함)과 `갑(甲)`(정상)을 가를 규칙이 없어 사람이 본다.
3. **길이 지시가 다른 지시와 충돌할 수 있다.** 5급이 평균 25자로 나온 건 같은 프롬프트가
   HSK 5 문법(`不仅…而且…`, `由…组成`)을 요구하는데 **그 문법을 넣으면 20자에 안 들어가서**다.
   2.5 는 문법을 흘려 길이를 지켰고 3.5 는 문법을 따르느라 길이를 어겼다.
   🔑 **모델은 지정 범위 중앙값보다 짧게 낸다** — 10-18(중앙 14)→12.6 · 14-20(중앙 17)→16.0.

### 🔑 서버 반영이 로컬과 다르다

`seed-official-decks` 는 캐시에 뜻이 2개 이상이면 ①② 병기를 넣는데, 그때 **예문도 캐시
것으로 바뀐다 — 이번에 517장(25%)**. 즉 로컬 `curationData.ts` 길이를 재고 "기존과 맞췄다"고
말하면 틀린다. 사용자가 보는 것은 병기가 적용된 서버 값이다.

---

## 2026-08-26 — 병기 규칙 결함 (완료) · 65덱 재시딩

- 상태: **완료**. `182ebe7`(규칙+테스트) · `5928d9f`·`46796e2`·`c75bf59`(문서) · 푸시까지.
- 카드 앞면 9건의 원인은 덱 데이터가 아니라 **시딩 규칙**이었다. 자세한 것은 위
  "남겨 둔 것" 의 B·C·D 항목에 적었다.
- 검증: 65덱 16,581행 · 빈 덱 0 · 뜻 빈칸 0 · `word_count` 불일치 0 · **제어문자 0** ·
  `is_published` 65 유지 · jest 1,196 통과(회귀 0) · adb 실기(NGSL `move`) 확인.

### 이번에 값을 치른 것 — 다음 세션이 그대로 겪는다

🔴 **인계 문서의 "안 됐다" 를 그대로 믿고 착수하면 헛돈다.** 아래 A-3 은 "zh 제목 번체화가
서버에 없다" 였는데 **이미 반영돼 있었다**(`c80477f` 커밋 본문에 그렇게 적혀 있다).
착수 전에 **서버를 한 번 떠 보는 데 1분**이면 된다.

🔴 **결함 목록의 길이는 조사 범위의 길이일 뿐이다.** "9건" 은 사다리 덱을 훑다 눈에 띈
표본이었고 실제로는 63덱 5,256행이었다. 원인이 규칙이면 규모는 **규칙이 닿는 범위**에서
세야 한다.

🔑 **큐레이션 탭 필터는 축이 둘이다** — 상단 칩(배우는 언어)과 별도의 `Meaning language`
드롭다운(뜻 언어). NGSL(en→ko)은 칩 English + 뜻 언어 Korean 이라야 목록에 뜬다.
칩만 바꿔 보고 "덱이 없다" 고 판단하지 말 것.

---

## ⏭️ 다음 세션이 이어받을 것

**A. 이번 작업에서 남은 것**

1. ~~앱 실기~~ ✅ 8/26 adb 로 확인(NGSL). 덱은 서버에서 오므로 새 빌드가 필요 없다.
2. **의심스러운 캐시 병기** — 첫 장부터 `按 = ① 따라서 ② 누르다 ③ 긴박하다` 가 있다.
   재생성이 만든 게 아니라 **캐시 병기** 가 넣은 것이라 "지어낸 뜻 판정" 트랙에서
   다뤄야 한다(내 판단만으로 지우지 않았다). `색` ③ "남녀 간의 애정…"(basic 덱 `color`
   카드 앞면)도 같은 트랙이다 — 8/26 규칙 수정으로도 남았다.
3. ~~`curated-ko-basic-zh-1` 제목 번체화~~ ✅ **오해였다.** 서버 `official_themes` 는
   제목·설명 모두 이미 번체다(`c80477f` 가 함께 반영했다). 남은 것은 **본문 500장이
   간체**라는 것뿐이고 그건 아래 5번이다.

**B. 1.7.0 트랙 (기한 순)**

4. 🔴 **Vertex 모델 `gemini-2.5-flash-lite` → `3.1-flash-lite`** — 은퇴 **2026-10-16**,
   은정님 결정으로 **9월 말** 전환. 배포가 아니라 `VERTEX_MODEL` 시크릿 하나(5분·롤백은 삭제).
   전환 당일 ① 실제 호출로 3.1 GA 판정(문서가 엇갈린다) ② 시크릿 ③ 산출물 확인(temp 0.4 라
   반복 필수) ④ 첫 주 Billing export. 코드 기본값까지 옮기면 `gemini-model-sync.test.ts` 의
   `EXPECTED_VERTEX_DEFAULT` 도 함께.
5. **덱 본문 번체화 → 그다음 국기**(`zh`=🇨🇳·`es`=🇪🇸). 국기를 먼저 바꾸지 말 것.
6. ~~**E4**(힌트 토글 후 글자 미복귀)~~ ✅ **2026-08-27 고침**(`64ea950`) — 8/27 빌드에 함께.
   남은 것은 **E5**(빈칸 박스 7.5dp)와 **동음이의어 병기 축소**다.
   - 🔴 **E5 는 원인이 가설이다.** 고치기 전에 `onLayout` 으로 박스 실제 높이를 찍을 것.
     손댈 자리가 `Pressable` 한 겹 감싸기라 **E3(빈칸 잘림) 회귀 위험**이 있다 — E3 는
     1.6.0 이 만든 것이라 이미 한 번 덴 자리다. 증상은 치우침이지 잘림이 아니다.
   - 🔴 **병기 축소는 작은 수정이 없다**(8/23 판정). `numberOfLines` 를 늘려도 207자는
     32dp 로 23줄이라 카드에 안 들어가 결국 축소된다. 근본 해법은 예문 카드와 같은 트랙
     (단계표+높이 판정+스크롤)이고 **원인 규명이 먼저**다.
9. **스킨 폰트가 안 먹는다** — `constants/skins.ts:67` 의 `y2k → jua` 는 정의만 있고,
   테마가 내보내는 `fontFamily`(`features/theme/context.tsx:44`)를 **실제로 쓰는 곳이
   `components/SkinSelector.tsx:55` 하나뿐**이다. 스킨 고르는 화면 미리보기에서만 Jua 가
   보이고 나머지는 전부 Pretendard 다. 하드코딩 `Pretendard_*` 가 **583곳 · 53개 파일**이고
   대부분 `StyleSheet.create` 정적 블록 안이라 `useTheme()` 를 못 읽는다 — 찾아바꾸기가
   아니라 스타일 구조를 바꾸는 일이다. **E4·E5 와 급이 다르므로 별도 트랙.**
7. **예문 중복 18건**(HSK 1급 14 · 베트남어 2 · 스페인어 1 · Basic Korean 1) — 서버 작업이라
   릴리스와 무관. ~~카드 앞면 결함 9건~~ ✅ **2026-08-26 완료**(`182ebe7` · 65덱 재시딩).
8. ⏳ **4~8주 뒤 ASO 재측정** — en 만 이름+설명을 같이 바꿔 원인을 못 가른다. 나머지 여섯이 대조군.

---

## 2026-08-26 — 레딧 "Nex" 는 실제 버그였다. 폰트 준비 전 렌더를 막았다

- 상태: ✅ 고침 · 실기 검증 완료 · **다음 릴리스에 포함시켜야 사용자에게 닿는다**
- 커밋: `0a59ab1`(게이트) · `8571e99`(회귀 가드) · `59046ac`(효과 없었음) · `13cf387`(온보딩 회귀 수정)

### 🔴 8/18 기록 정정 — "재현되지 않았다"는 틀렸다

이 문서 위쪽 `2026-08-18` 절의 *"`Nex`는 현재 소스의 오타로 재현되지 않았으며"* 는 **틀렸다.**
Galaxy S22 에 preview 빌드를 올려 **100% 재현**했다. 소스의 문자열은 `Next` 가 맞고,
잘리는 것은 **렌더 단계**다.

### 원인

RN Android 는 Text 의 폭을 **그 시점의 Typeface** 로 잰다. 앱은 스플래시로 가린 채 트리를
먼저 그렸는데, 그때 커스텀 폰트가 아직이면 시스템 폴백(Roboto·OneUI Sans KR)으로 잰 폭이
상자에 굳는다. Pretendard 가 더 넓어 나중에 진짜 폰트로 그리면 글자가 상자를 넘고,
띄어쓰기가 있으면 마지막 낱말이 안 보이는 둘째 줄로 넘어가고 없으면 마지막 글자가 깎인다.

🔴 **가려져 있어도 측정은 남는다.** RN 의 텍스트 측정 캐시는 문자열과 스타일로만 키를 잡아
잘못 잰 값을 프로세스가 끝날 때까지 다시 쓴다. **재마운트로는 못 고친다** — 화면 크기를
바꿔 재레이아웃을 강제해도 상자 폭이 한 픽셀도 안 변했다.

🔑 **홈 화면은 무사했다.** 인증 hydrate 를 기다렸다 그려져 그때는 폰트가 준비돼 있다.
**즉시 그려지는 로그인·온보딩만** 당하므로 평소 경로로는 절대 안 걸린다.

### 실측 (Galaxy S22 · 로그아웃 콜드 스타트 · 3회 반복)

| | 수정 전 | 수정 후 |
|---|---|---|
| 온보딩 `Next` | **`Nex`** · 글자폭 86 | `Next` · **104** |
| `Google로 로그인` | `Google로` · 상자 327 | 온전 · 334 |
| `계속하면…간주합니다` | `간주합니디` · 712 | 온전 · 736 |
| `개인정보 처리방침` | 앞뒤 깎임 · 232 | 온전 · 240 |
| `A Voca Do` | `A VOCA DC` · 272 | 온전 · 281 |

폰트 역산이 결정적이었다: `A VOCA DO` 상자 272 = **Roboto 199.7 + 자간 8칸 72 = 271.7**
(Pretendard 는 281 을 요구). 자간은 마지막 글자 뒤에 안 붙는다.

### 🔴 게이트가 낳은 회귀 — 최초 실행 온보딩이 통째로 사라졌다

게이트 없는 대조군 **3/3 온보딩** · 게이트 있는 판 **3/3 로그인**.

🔴 **effect 가드로는 못 막았다(`59046ac` 는 효과가 없었다).** 로그인으로 보내는 건
`app/(tabs)/_layout.tsx` 의 **선언형 `<Redirect href="/login" />`** 이고 루트의 온보딩 이동은
effect 다. 같은 커밋이면 Redirect 가 이긴다. 게이트 전에는 이 레이아웃이 `loading` 중이라
빈 화면을 돌려주는 동안 루트 effect 가 먼저 움직여 **우연히** 온보딩이 이겼을 뿐이다.
`13cf387` 에서 판단을 그 선언형 자리로 옮겼다.

🔴 이 회귀는 **기존 사용자에게 안 보인다** — 온보딩 플래그가 이미 true 라 앱을 지우고 새로
깔아야 드러난다. `__tests__/font-gate-before-render.test.ts` 가 게이트와 분기 순서를 함께 지킨다.

### 곁다리 (미해결) — Y2K 스킨의 Jua 폰트가 안 먹는다

`constants/skins.ts:67` 은 Jua 인데 실제로는 Pretendard 가 그려진다. 콜드 스타트해도 폭이
클래식과 한 자도 안 다르다. 화면들이 스타일시트에 `Pretendard_*` 를 직접 박아 스킨 값을
덮기 때문이다 — 홈 화면만 봐도 하드코딩 27곳 대 스킨 사용 2곳.

### 레딧 답변 (원고 A·B 게시 완료 — 2026-08-27)

두 스레드 모두 원고대로 게시했다. 방침은 **약속을 하나만 남긴다**(`Nex` 수정).

✅ **그 약속은 1.6.1 로 닫혔다(같은 날 저녁).** 스레드에 한 줄로 알렸다 —
*"the \"Nex\" button is fixed in 1.6.1, which is in review now"*. 🔑 여기에 Day 잠금·E4 를
얹지 않았다: **그 제보자가 지적하지 않은 수정을 나열하면 답이 아니라 홍보가 된다**
(Day 잠금은 다른 사람이다). `그러다` 예문도 꺼내지 않았다 — 꺼내는 순간 약속이 따라 붙는다.

✅ **Day 잠금 제보자에게도 답했다(2026-08-27).** 약속 0건. 🔑 문안에서 두 가지를 지켰다:
⑴ **사과가 먼저**(며칠 기다린 사람이다) + `That's on me` 로 배경을 변명에서 분리,
⑵ **"Day 7 을 다시 할 필요 없다"를 반드시 넣는다** — 이미 40/40 을 외운 사람이라 그것이
지금 가장 궁금한 것이다. 🔴 `You are the best!` 류 상투어는 뺐다. 바로 앞의
*"I wouldn't have found it on my own"* 이 **사실이라서 더 강한 감사**인데, 상투어를 얹으면
그 구체성이 씻긴다.

- 🔴 `그러다` 예문은 **서버에 그대로다.** 사다리 덱은 신규 1,686 + **재사용 1,482** 인데
  `usedForm` 검사는 신규 생성 경로에만 있다. **몇 장인지 세기 전에는 고치겠다고 하지 말 것.**
- 🔴 덱 상태는 **번들이 아니라 서버**로 확인한다. `curationData.ts` 에는 옛 값이 남아 있다.
  덱 id 는 옛 이름 그대로다(`curated-ko-intermediate-1` = 새 `Intermediate Korean I`).
- 안 하기로 한 것: 예문 단어 탭→카드(6개 언어 형태소 분석) · 예문 난이도 손질 ·
  중복 제거 도구(입력 중 `Also in 'X'` 로 이미 충족) · CSV 재조사(앱은 무죄)

## 2026-08-27 — 다 외운 Day 가 잠긴 채 남던 것 (완료 · 릴리스 대기)

레딧 새 댓글: Basic Korean 500 의 Day 7 이 **40/40 암기인데** Day 8 이 "이전 Day 를 먼저
완료하세요" 로 며칠째 잠겼다. 스크린샷상 280/500 = 정확히 7일 × 40 → `planCurrentDay` 가
7 에 멈춰 있다는 뜻이다. ✅ `416d095` 로 고쳤고 **커밋만 되어 있다(릴리스 필요)**.

### 원인 — 같은 사실이 두 벌로 저장돼 있었다

"다 외웠다"는 단어의 `isMemorized`, "완료했다"는 리스트의 `planCurrentDay` 카운터다.
화면은 위에서 첫째를 읽어 "40/40 암기"라 쓰고 아래 버튼에서 둘째를 읽어 "먼저 완료하세요"
라고 쓴다. 둘이 어긋나면 맞출 방법이 없다.

카운터를 올리는 코드는 앱 전체에 두 줄뿐이고(`flashcards/screen.tsx:370`,
`quiz/screen.tsx:227`) 둘 다 `finishSession` 안에만 있다. 그래서 세 갈래로 샌다.

1. **예문 모드는 `planDay` 를 읽지도 않았다.** 계획 화면이 Day 마다 내놓는 세 모드
   (`plan/screen.tsx:566-568`) 중 하나가 막다른 길이었다. 조건 없이 100% 재현된다.
2. **중도 이탈**은 암기 전환만 커밋하고(`18986c0` 에서 의도한 동작) 진행도는 안 올린다.
   40개 다 넘기고 마지막 카드 직전에 나가면 "40/40 암기 + Day 미완료" 가 만들어진다.
3. 그 비율의 **분모가 Day 의 단어 수가 아니라 "이번 세션에서 본 카드 수"** 다. 배치
   오버레이의 `onFinish` 로 10/40 만 보고 끝내면 반대로 **25% 만 외운 Day 가 완료**된다.

### 고친 방식 — 경로마다 막지 않고 축을 바꿨다

`deriveUnlockedDay(words)` 는 암기 비율이 기준(50%, 홈 카드 `evalDay` 와 같은 값)에 못
미치는 가장 낮은 Day 를 돌린다. 어떤 모드로 외웠든, 중간에 나갔든 상관없어진다.

잠금 기준은 `max(planCurrentDay, deriveUnlockedDay(words))` 다. `planCurrentDay` 는
`MAX()` 로만 오르므로(`features/vocab/db.ts:891`) 이 조합은 **열기만 하고 절대 잠그지
않는다** — 이미 진도가 나간 사람이 업데이트 후 새로 잠기는 회귀가 원천 차단된다.

- 🔴 **유도값을 `computePlanStatus` 에 먹이면 안 된다.** 완료 판정이
  `planCurrentDay > planTotalDays`(`engine.ts:66`) 라 계획이 저절로 끝나 버린다. 잠금에만.
- 규칙 본체는 `engine.ts` 의 `isDayLocked` 한 곳에 둔다. 화면과 테스트가 각자
  `viewingDay > ...` 를 들면 화면만 바뀌어도 테스트는 계속 통과한다.
- 예문 모드 `planDay` 배선은 잠금과 **별개로 여전히 필요**하다. `planUpdatedAt` 이 안
  움직이면 홈 카드가 "오늘 달성" 으로 안 가고, `computePlanStatus` 의 유휴 판정(7일)에
  걸려 **매일 공부한 사람에게 "7일 이상 학습하지 않았습니다"** 배너가 뜬다.

### 이번에 틀렸던 것

🔴 **`computeCurrentDay` 를 "이미 있는 규칙" 이라며 재사용하려다 틀렸다.** 이름과 위치가
딱 맞아 보였지만 본문은 `dayWords.some(w => !w.isMemorized)` — **100% 기준**이다. 그대로
썼으면 39/40 에서 갇히는 부류를 새로 만들 뻔했다. dead code 가 옆에 있으면 근거로 삼기
전에 **본문을 읽을 것**(주석으로 못 박아 뒀다).

🔴 첫 진단에서 "50% 가 두 개이고 서로 다른 것을 지배한다" 고 했는데 **과장이었다.**
`updatePlanProgress` 가 `planCurrentDay` 와 `planUpdatedAt` 을 함께 세우므로 둘은 같은
게이트를 쓴다. 도움말이 설명하는 `evalDay` 의 50% 야말로 `isStudiedToday &&
planCurrentDay === 1` 한 갈래에서만 쓰이는 **사실상 죽은 코드**다.

### 조사에서 쓸 만했던 것

- 🔴 **게스트 제보자는 서버에서 못 찾는다.** `cloud_lists` 를 다 뒤져도 13일·500단어·280암기
  조합이 없었다(계획을 가진 리스트가 클라우드에 25개뿐). 개인 특정은 포기하고 증상
  재현으로 갔다. 같은 상태의 클라우드 계정은 25개 중 2개.
- 🔑 **스크린샷에서 상태를 역산할 수 있다.** 배너가 "Study in Progress" 이고
  `remaining=6`·`totalDays=13` 이면 시작이 7~8일 전인데, `planUpdatedAt` 이 NULL 이면
  유휴 판정에 걸려 "7일 이상 학습하지 않았습니다" 가 떠야 한다. 안 떴으므로 그 값은 최근에
  갱신됐다 = 예문 전용 가설은 약하다.

### 검증

옛 규칙(`viewingDay > planCurrentDay`)으로 되돌리면 "Day7을 다 외웠으면 Day8은 열린다"가,
유도값만 쓰게 하면 "열기만 하고 잠그지는 않는다"가 각각 깨진다. 양방향 다 잡힌다.
전체 1,212개 통과(실행 실패 3개는 CLAUDE.md 에 적힌 기존 건). 🔴 **실기 재현·검증은 안 했다.**

### 범위 밖으로 뺀 것

`evalDay` 의 죽은 50% 와 그걸 설명하는 도움말 문구(`home.dayStatusHelpMessage`:
"50% 이상을 암기하면 카드가 '오늘 달성'으로 이동됩니다"). 홈 카드를 실제로 움직이는 건
`planUpdatedAt` 이라 문구가 맞지 않는다. 이번 증상("며칠째 진도가 안 나감")이 아니므로
별건으로 둔다.

## 2026-08-27 — 1.6.1 실기 검증 (Galaxy S22 · preview `dd4f929`)

네 항목 전부 통과했다. **adb 로 직접 몰아서 쟀다** — 절차는 [[env_adb_ui_automation]].

| # | 항목 | 결과 |
|---|---|---|
| ① | 온보딩 최초 실행 | ✅ **4/4** (로그인 직행 0회) |
| ② | 글자 끝 잘림 | ✅ 온보딩 4쪽·홈·단어장·학습 화면 전부 온전 |
| ③ | Day 잠금 | ✅ 예문 모드·중도 이탈 두 경로 다 |
| ④ | E4 힌트 토글 | ✅ 실제 축소가 일어나는 조건에서 |

### ③ 은 두 갈래로 갈라서 봐야 한다

**기준선을 먼저 잡는다** — 계획을 막 만든 직후 Day 2 는 "이전 Day를 먼저 완료하세요" 다.
이 상태를 못 보고 시작하면 무엇이 고쳐졌는지 말할 수 없다.

- **예문 모드**: Day 1 을 예문 학습으로 완주 → Day 2 가 열린다. `planDay` 배선 확인.
- 🔑 **중도 이탈이 유도 규칙의 진짜 증명이다.** Day 2 를 3문항 중 2개만 풀고 하드웨어
  뒤로가기로 나간다. 카운터는 2 에 머무는데(완주 안 함) Day 2 가 2/3(66%) 이므로
  **Day 3 은 열리고 Day 4 는 잠긴다.** 열기만 하고 전부 열지는 않는다는 것까지 한 번에 보인다.

### ④ 는 조건을 만들어야 한다

🔴 **짧은 예문으로는 E4 를 검증할 수 없다.** 힌트를 켜도 넘치지 않아 축소가 애초에 안 일어나고,
그러면 "끄니까 돌아왔다"는 아무것도 증명하지 못한다(처음에 유행어 덱으로 그럴 뻔했다 —
그 덱은 예문이 **최대 22자**다).

🔑 **서버에서 긴 예문 덱을 찾아 쓴다.** `official_words` 를 `char_length(example_en)` 로
정렬하면 된다. 쓴 것은 "노인 의학 및 노화 건강"(30단어·최대 212자).

실측(문장 높이): 힌트 끔 **360**(4줄×90px=단계1) → 켬 **234**(3줄×78px=단계2) →
끔 **360 정확히 복귀** → 켬 234 → 끔 360. 두 번째로 켤 때 계단을 안 밟고 곧장 234 로
가는 것이 `enterHint` 가 의도한 동작이다.

### adb 로 몰 때 이번에 걸린 것

- 🔴 **Git Bash 가 `/sdcard/ui.xml` 을 윈도우 경로로 바꾼다**(`/Files/Git/sdcard/...`).
  `MSYS_NO_PATHCONV=1` 를 앞에 붙여야 `uiautomator dump` 결과를 읽을 수 있다.
- 🔴 **눈 감고 탭하면 엉뚱한 걸 누른다.** 좌표만 보고 누르다 마이크 권한 요청을 띄웠고
  (음성 입력 버튼), 단어 추가 폼에서는 자동완성이 `serendipity` 를 `serendipity berry` 로
  바꿔 놨다. 폼과 씨름하는 대신 **서버에서 조건에 맞는 덱을 찾는 쪽이 훨씬 빨랐다.**
- 🔑 **첫 실행 재현은 `pm clear` 로 반복한다.** 재설치보다 훨씬 빠르고 같은 경로를 탄다.
- 🔑 uiautomator 덤프는 **전환 애니메이션 중에 옛 화면을 준다.** 온보딩 3·4 쪽이 같은
  내용으로 찍힌 것이 그것이다 — 화면이 바뀐 줄 알았는데 덤프가 안 따라온 것뿐이었다.

### 곁가지 — 안 고친 것

온보딩 네 쪽 모두 **페이지 인디케이터 점이 부제 마지막 줄에 겹친다**(부제 하단 y=1873,
점 y≈1883). 1.6.1 이 만든 것이 아니고 출시를 막을 정도도 아니라 두었다.

## 2026-08-27 — 학습 기능 전반 검토 (검토만 · 미착수)

Day 잠금이 드러낸 결함의 **유형**으로 학습 기능 전체를 훑었다: *유도할 수 있는 사실을
별도 컬럼에 저장하고, 그 컬럼을 한쪽 경로에서만(또는 엉뚱한 경로가) 갱신한다.*

### 🔴 1. "마지막 학습"이 학습과 무관하게 갱신된다 — 유일하게 실사용 영향

`lastStudiedAt` 을 쓰는 곳이 7개인데 **학습 세션은 하나도 없다**:
`updateWord` · `toggleMemorized` · `toggleStarred` · `mergeLists` · `setWordsMemorized` ·
`copyWords` · `moveWords` (`features/vocab/db.ts`).

🔴 **`updateStudyTime()`(`db.ts:669`)이 학습용으로 만들어져 있는데 아무 데서도 안 부른다.**
이름이 맞는 유일한 함수가 죽어 있다.

실기 확인: 덱을 **담기만 하고 학습은 0**인 시점에 단어장 목록이 "마지막 학습: 방금 전"이라
표시했다. 별표 하나만 눌러도 같다.

⚠️ **1.6.1 에 넣지 않기로 했다.** 홈 정렬·단어장 목록·운영 쿼리가 모두 이 컬럼을 물고 있어
파급 범위를 먼저 세야 한다. [[project_dau_query_method]] 의 "학습 지표가 아니다"가 여기서 나왔다.

### 🟡 2. `lastResult*` 세 컬럼은 아무도 안 읽는다

`saveLastResult` 가 세션 완주마다 **단어장 전체 암기율**을 계산해
`lastResultMemorized/Total/Percent` 에 저장하는데, **화면에서 읽는 곳이 0곳**이다(사용처는
sync 와 DB 매퍼뿐). 부작용: ⑴ 완주마다 아무도 안 보는 값 때문에 리스트가 dirty 로 찍혀
클라우드 push 가 일어난다 ⑵ 이름은 "마지막 학습 결과"인데 담기는 건 전체 암기율이라,
나중에 누가 화면에 쓰면 그 순간 거짓이 된다. 마이그레이션 011 로 들어왔다.

### 🟡 3. 완주와 이탈의 학습량 기록 지점이 다르다

완주는 `app/study-results.tsx:95`, 이탈은 `use-abandon-record.ts` 의 언마운트 cleanup 이다.
`completedRef` 플래그로 이중 기록을 막는다. 지금은 맞게 돌지만 **Day 잠금이 났던 구조와
같다** — 경로마다 기록 지점이 따로라 새 경로가 생기면 조용히 빠진다(예문 모드가 실제로
그랬다). `useSessionCommit` 은 이미 세 경로를 모았는데 **학습량 기록만 아직 안 모였다.**

### ✅ 4. 결함이 아닌 것 — 낭독(autoplay)

낭독은 암기·학습일·마지막 학습·계획 진도 **전부 기록하지 않는다.** 결함으로 보고 파다가
`use-abandon-record.ts` 주석에서 확인했다 — *"오토플레이는 기존 결정대로 통계 제외"*.
내린 결정이다. 다만 **낭독으로만 공부한 날은 스트릭이 끊긴다** — 결정을 유지할지는 별개.

### 🔴 5. 모드별 계측이 여전히 없다

제보자가 세 경로 중 어디로 갇혔는지 못 가른 이유다. 같은 종류의 버그가 또 나면 또 못 가른다.

## 2026-08-27 — 1.6.1 iOS 실기 검증 (TestFlight build 39 · iPhone)

네 항목 전부 통과했다. Android preview(`dd4f929`)에 이어 iOS 에서도 확인했으므로
**이 릴리스의 실기 검증은 끝났다.**

| # | 항목 | 결과 |
|---|---|---|
| ① | 온보딩 최초 실행 | ✅ 삭제 후 재설치에서 온보딩 4쪽이 떴다 |
| ② | 글자 끝 잘림 | ✅ |
| ③ | Day 잠금 | ✅ **양방향 다** (아래) |
| ④ | E4 힌트 토글 | ✅ |

### 🔴 "iOS 온보딩은 TestFlight 로 검증할 수 없다"는 틀렸다

이 문서가 그렇게 적어 두었지만(8/23 실측 근거), **앱을 삭제하고 다시 받으면 된다.**
그렇게 해서 통과를 확인했다.

게다가 그 전제 자체가 조건부였다. **정상 업그레이드(1.6.0 → 1.6.1)는 컨테이너를 유지한다.**
업데이트 직후 온보딩이 안 뜨고 로그인 화면이 나온 것이 그 증거다 —
`@soksok_onboarding_done` 이 `'true'` 로 남아 있었고 단지 로그아웃 상태였던 것이다
(로그아웃은 `@soksok_auth` 만 지우고 온보딩 기록은 남긴다 · `features/onboarding/store.ts`).
8/23 에 데이터가 날아간 것은 **다운그레이드(1.6.0 → 1.5.0)를 낀 왕복**이었다.
두 경우를 "빌드 교체는 지운다" 한 줄로 묶어 읽으면 검증 경로를 하나 잃는다.

🔑 **업데이트 후 온보딩이 안 뜨는 것은 정상이다.** 이 회귀는 신규 설치자만 겪는다.
"업데이트 첫 실행 = 신규 설치 조건"이라고 읽은 것이 잘못이었고, 은정님이 그 자리에서
바로잡았다.

### ③ 은 iOS 에서 양방향이 다 나왔다

Android 에서는 "50% 를 넘기면 열린다" 쪽만 봤는데, iOS 에서 반대쪽까지 나왔다.

- **안 열리는 쪽**: 하루 10개 계획의 Day 2 에서 3개만 맞히고 이탈 → 3/10 = 30% 라
  Day 3 은 잠긴 채. 기준이 실제로 걸린다는 증거다.
- **카운터 없이 열리는 쪽**: 같은 덱으로 계획을 다시 세우면 `savePlan` 이
  `planCurrentDay = 1` 로 초기화하는데(`features/vocab/db.ts:863`), 이미 외운
  Day1·Day2 가 100% 라 **Day3 까지 열렸다.** 완주 카운터가 1인데 Day3 이 열린 것 =
  잠금이 카운터가 아니라 단어 상태에서 계산되고 있다는 직접 증거다.
  고치기 전이었다면 여기서 Day2 가 잠겼다 — 제보자가 겪은 그 상태다.

## 검토 — 100% 외운 Day 도 학습하고 넘어가야 하지 않나 (은정님 질문 · 검토만)

위 "카운터 없이 열리는 쪽"을 보고 나온 질문이다. **미착수. 1.6.1 에 넣을 것이 아니다.**

지금 규칙은 `max(완주 카운터, 암기 50% 유도값)` 이라 **어느 한쪽만 만족해도 열린다.**
여기서 서로 다른 두 증상이 만난다.

| | 증상 | 누가 겪나 |
|---|---|---|
| A | 학습을 **했는데도** 안 넘어감 | 레딧 제보자 |
| B | 학습을 **안 했는데** 넘어감 | 은정님이 짚은 것 |

1.6.1 은 A 를 고치면서 **B 를 대가로 받아들였다.**

### B 가 생기는 경로는 넷이고, 그중 하나는 오히려 열려야 한다

1. 계획을 다시 세웠을 때(암기 상태 유지) ← 은정님 사례
2. 단어 목록·상세에서 암기를 수동 토글했을 때
3. **계획 밖 학습**(전체·골라서·검색 후 학습)으로 그 단어들을 외웠을 때
4. 같은 단어가 다른 단어장에서 이미 외워진 뒤 가져왔을 때

🔑 **3 은 열리는 것이 맞다.** 사용자는 분명히 학습했고 계획 화면을 안 거쳤을 뿐이다.
이걸 진도로 안 쳐주면 "공부했는데 왜 안 넘어가냐"는 A 의 변종이 된다.
어색한 것은 1·2 다 — 특히 1 은 사용자가 방금 "다시 세우겠다"고 선언한 직후다.

### 🔴 잠금을 세션 기준으로 되돌리는 안은 권하지 않는다

A 의 원인이 **완주 카운터 자체가 새는 그릇**이었기 때문이다(예문 모드가 `planDay` 를
읽지도 않음 · 중도 이탈 시 안 오름 · 분모가 Day 단어 수가 아니라 세션에서 본 카드 수).
잠금을 다시 그 카운터에 걸면 제보자가 며칠 갇혔던 상태로 돌아간다.

### 진짜 쟁점은 잠금이 두 일을 겸하는 것

- **안전장치** — 준비 안 된 Day 로 뛰어드는 것을 막는다
- **진도 표시** — 지금 어디까지 왔는지 알린다

열려 있는 Day1~3 을 보고 이상하다고 느끼는 것은 **잠금이 진도로 읽히기 때문**이다.
실제로는 안전장치로만 동작한다. 이미 외운 Day 를 잠글 이유는 안전장치 관점에서 없다.
그렇다면 고칠 대상은 잠금 규칙이 아니라 **진도를 보여주는 방법**일 수 있다.
지금 Day 칩은 열림/잠김 두 상태뿐이라 Day1(이미 외움)과 Day3(오늘 할 것)이
화면에서 똑같아 보인다.

### 세 갈래 (권고: ② 또는 ③ · 잠금 규칙은 건드리지 않는다)

| 안 | 내용 | 성질 |
|---|---|---|
| ① | 그대로 둔다 | B 의 어색함이 남음 |
| ② | Day 칩을 3상태로(이미 외움 / 오늘 할 것 / 아직) | 잠금 불변 → A 재발 없음 |
| ③ | **재계획 시 기본 필터를 "미암기"로** | 1 번 사례 직접 해결 |

③ 은 이미 있는 기능이다 — 계획 만들기 모달의 `전체 / 미암기 / 암기` 필터에서
"미암기"를 고르면 외운 단어가 빠지고 남은 것만 Day1 부터 재배정된다
(`features/vocab/mutations.ts:342` `setupPlan`). 즉 **기본값 문제**일 수 있다.
처음 세울 때는 어차피 다 미암기라 차이가 없고, 다시 세울 때만 갈린다.

## 다음 세션 착수점

### 0. 1.6.1 — ✅ 빌드·TestFlight·실기 검증까지 전부 끝났다 (2026-08-27)

✅ **EAS Starter 구독 완료**(은정님, 8/27). 그 직후 양 플랫폼 production 을 구웠다.

| | 상태 | 버전 | 커밋 |
|---|---|---|---|
| iOS | ✅ FINISHED · TestFlight 배포 · **실기 4/4** | 1.6.1 · build **39** | `98e30ca` |
| Android | ✅ FINISHED (AAB) · preview 실기 4/4 | 1.6.1 · vc **26** | `98e30ca` |

✅ **TestFlight 업로드까지 끝났다**(`eas submit --platform ios --profile production --latest`).
제출 id `848788e8`. ✅ **iOS 실기 검증도 네 항목 전부 통과했다**(위 절 참조 — 온보딩은
**앱 삭제 후 재설치**로 확인).
**남은 것은 Play Console 업로드부터다. 스토어에 올릴 준비가 다 됐다.**

🔴 **versionCode 가 25 가 아니라 26 이다.** 할당량에 막혀 죽은 시도가 번호를 한 번 올려
두고 죽었다. Play 프로덕션이 24 라 문제는 없고 **25 는 영구 결번**이다.

🔴 **`eas build` 는 실패해도 종료 코드 0 을 돌려준다.** 할당량 초과로 빌드가 시작조차 못
했는데 exit 0 이라 "진행 중"으로 오독했다. **반드시 로그 본문의 `Error: build command
failed.` 를 볼 것.** 이번엔 큐 진입을 따로 감시하는 백그라운드 작업을 하나 더 걸어
`build:list` 로 교차 확인했다 — 그 방식이 맞다.

### 1. 그다음 순서

0. ✅ **1.6.1 배포 완료 — 닫힌 건이다(2026-08-28).** 양 스토어 심사 통과 후 배포 중.
   iOS build 39 · Android vc26. 빌드·검증·제출·머지·레딧 답글까지 전부 끝났다.
   🔑 **8/27 저녁 빌드 → 8/28 배포.** EAS Starter 구독이 병목을 풀어 하루 만에 나갔다.
   AAB 링크(보존): https://expo.dev/artifacts/eas/M8wb_eZKwIMSYb6u_HEmTa6AsCM7gXT21_Qb7k06cRA.aab
   🔑 `build:list --json` 최상위에는 URL 이 없다 — `artifacts` 안에 있다.
   🔑 레딧 답글은 `in review now` 로 써 뒀지만 **고칠 필요 없다** — 시점을 안 타게 쓴 문안이다.
### 🔴 다음 트랙 — 순서는 은정님이 정했다 (2026-08-28): ①`그러다` → ②마지막 학습일 → ③100% Day

#### ① `그러다` 예문 — ✅ **세었다 (2026-08-28). 사다리 4덱 전량 3,168장.**

증상: 예문이 표제어 대신 다른 낱말을 쓴다(레딧 제보 — `그러다` 카드 예문에 `그렇게`).

**결과: 표제어가 예문에 안 쓰인 것 34장(1.1%). 그중 제보자가 지적한 유형은 12장이다.**

| | 장수 | 무엇인가 |
|---|---|---|
| **A. 표제어가 예문에 아예 없다** | **12** | 제보자가 본 그 결함. **전부 재사용분** |
| B. 어근은 같고 형태가 다르다 | 16 | 능동↔피동·사동(`포함하다`→`포함되어`), 명사형(`진출하다`→`진출을`) |
| C. 사실상 정상 | 6 | 분리 사용(`준비하다`→`시험 준비를 하고`), 부사 파생(`철저하다`→`철저히`) |

출처별로는 **재사용 23장 / 1,483 (1.6%) · 신규 11장 / 1,685 (0.7%)**.

**A 12장 전량** (덱 · 표제어 → 예문이 대신 쓴 말)

| 덱 | 표제어 | 뜻 | 예문이 대신 쓴 말 |
|---|---|---|---|
| Int-I | `그러다` | to do so | **그렇게** ← 제보자가 찾은 것 |
| Int-I | `새롭다` | new | 없음(`신기술 도입은…`) |
| Int-I | `비하다` | to compare | 비교했을 |
| Int-I | `올리다` | to raise, to upload | 게재하여 |
| Int-I | `불과하다` | to be only | 뿐인데 |
| Int-I | `성장` | growth | 성숙 |
| Int-I | `골` | gist, core | 핵심 |
| Int-I | `희다` | to be white | 하얗게 · 순백 |
| Int-I | `문화재` | cultural property | 숭례문 |
| Adv | `들이다` | to let in | 도입 |
| Adv | `뛰어나다` | outstanding | 돋보이는 |
| Adv | `이어` | successively | 없음 |

🔑 **A 12장 중 9장이 Intermediate I 한 덱에 몰려 있다**(Adv 3 · Basic 0 · Int-II 0).
Int-I 은 786장 중 700장이 재사용분이라 재사용 비중이 가장 높은 덱이다.

#### 🔴 "신규분은 검사를 통과했으니 안전하다"는 절반만 맞았다

착수 전 전제는 **재사용 1,482장만 위험**이라는 것이었다. 실제로 A 12장은 전부 재사용분이
맞다. 그러나 **신규분에서도 11장이 걸렸고**, 그중 7장은 모델이 `usedForm` 으로
**명사형을 스스로 보고했는데 검사가 통과시켰다** — `특정하다`→`특정` · `예상하다`→`예상과` ·
`진출하다`→`진출을` · `고생하다`→`고생`. 검사는 "표제어 문자열이 usedForm 에 들어 있나"만
보므로 **품사가 바뀐 것을 못 가른다.** 다행히 A 급으로는 하나도 안 갔다(어근은 다 보인다).

🔴 부수 발견 — **예문 자체의 문법·띄어쓰기 오류가 따로 있다.** 표제어 사용 여부와 무관해
이번 집계에 안 잡힌다: `책임을지는`(신규 `책임지다`) · `깔음으로써`(→깖으로써) ·
`굶다 시피`(→굶다시피) · `관심이 별로 띄지 않는`(비문). **규모는 안 셌다.**

#### 어떻게 셌나 — 기존 검사로는 못 센다

`exampleUsesTerm` / `ko-deck-checks.ts:127` 은 용언 어간을 못 찾으면 `term.slice(0,1)` 한
글자로 완화해서 **"그"가 "그가 그렇게"에 걸려 `그러다`를 통과시킨다.** 그래서 활용 판정기를
새로 썼다(스크래치패드 `conj.py`, 저장소 미반영).

- 어간 앞부분은 **그대로** 요구하고, 마지막 음절만 초성 일치 + 중성 축약/불규칙 후보를 허용
- 불규칙 전부: ㅂ·ㄷ·ㅅ·ㅎ·르·우 불규칙, 으탈락·ㄹ탈락, 여불규칙(`하`→`해`), 존칭(`계시`→`계세`)
- 🔑 **결정적인 것은 받침 제한이었다.** 어간에 없던 받침을 무제한 허용하면 `그러`+`렇`이
  매칭돼 `그러다`를 또 놓친다. 어미가 붙여 줄 수 있는 받침은 `ㄴ ㄹ ㅁ ㅆ ㅂ` 뿐이고
  **ㅎ 은 거기 없다** — 이 한 줄이 제보 건을 잡아냈다.
- 검증: 알려진 정답/오답 22 케이스 전부 통과. **역검증**으로 통과분 용언 1,037장에서
  35장을 뽑아(어간 짧은 순 20 + 무작위 15) 눈으로 봤고 **놓침 0**.

#### ✅ 고쳤다 (2026-08-28) — 47장. 서버·번들·생성 json 세 곳 반영 완료

| 무엇 | 장수 | 어떻게 |
|---|---|---|
| A. 표제어가 예문에 아예 없던 것 | 12 | 예문 재작성 |
| 문법·맞춤법·띄어쓰기 | 32 | 대부분 부분 수정 |
| 병기 문장 중 하나가 표제어를 안 쓴 것 | 3 | 해당 문장만 교체 |

**B 급 16장(능동↔피동 `포함하다`→`포함되어`, 명사형 `진출하다`→`진출을`)은 고치지 않았다** —
어근이 보이므로 학습자가 표제어를 못 찾지는 않는다. 서버에 9장이 남아 있고 그게 정상이다.

🔴 **"예문 재생성 금지" 방침과 충돌하지 않는다.** 그 방침은 예문이 어렵다·문체가 섞였다는
**품질·취향** 문제로 수천 장을 갈아엎는 것을 막으려던 것이다. A 급은 취향이 아니라 카드가
제 기능을 못 하는 것이고(`그러다` 카드에 `그러다`가 없다), 47장이다.

**문법 오류는 AI 로 세었다** — 3,168장 전량 2회 판정 후 교집합(35건) + 한쪽만(15건)을
전부 눈으로 갈라 **32건 채택 · 18건 기각**. 기각한 것: 판정이 갈리는 어색함(`지속적 투자`),
문체 취향(`대개는 초기에는`), 표제어가 사라지는 수정(`인제`→`이제`), 그리고
🔴 **정상인데 AI 가 틀린 것**(`손때가 밴`을 "배인"으로 고치라고 했다 — `밴`이 맞다).
🔑 한 번만 물었으면 44건 중 9건이 근거 없이 들어왔다. **두 판정의 교집합부터 보는 것이 맞다.**

🔴 **`이러다`는 부분 수정이 불가능했다.** `이러나는`을 `일어나는`으로 고치면 표제어가 사라진다
— 원래 예문이 `일어나다`를 쓰려다 오타 낸 것이고 표제어와는 처음부터 무관했다. 전면 재작성.

🔴 **카드 전체로 세면 병기 항목을 놓친다.** ①②③ 이 붙은 305장을 **문장별로** 다시 재니
3장이 더 나왔다 — `업체` 는 ②(`특정 업체만`)로 통과했지만 ①은 여전히 유의어(`중소기업`)였다.
`희다` 는 내가 ①만 고치고 ②(`순백의`)를 남겨 둔 것이 이 검사에 걸렸다. **뜻마다 붙은
문장은 각각 표제어를 써야 한다.**

**검증**: 수정안 47장 전부 판정기 통과 · 문법 재판정 2회 모두 0건 · 반영 후 서버 재조회에서
카드 기준 9장(=B 급)·문장 기준 0장. 덱별 길이 기준(Basic 14 / Int 30 / Adv 45자) 유지.

#### ✅ 판정기를 저장소에 넣었다 — 다음 덱부터 자동으로 걸린다

`scripts/lib/ko-conjugation.ts` 신설 + `ko-deck-checks.ts` 의 '예문에 표제어 없음' 교체.
**advisory 를 뗐다** — 지금까지 exit 0 이라 아무도 보지 않았다.

- 🔑 **결정적인 규칙은 받침 제한.** 어간 앞부분을 그대로 요구하고 마지막 음절만 초성 일치 +
  중성 축약/불규칙으로 여는데, 거기서 받침을 무제한 허용하면 `그러`+`렇`이 매칭되어 `그러다`를
  **또** 놓친다. 어미가 붙여 줄 수 있는 받침은 `ㄴ ㄹ ㅁ ㅆ ㅂ` 뿐이고 **ㅎ은 거기 없다.**
- 불규칙 전량(ㅂㄷㅅㅎ르우·으탈락·ㄹ탈락·여불규칙 `하`→`해`·존칭 `계시`→`계세`) + 합성 용언
  분리 사용(`낯가리다`→"낯을 가려서") + 여러 어절 표제어(`몸을 풀다`).
- ⚠️ **알려진 한계**: 표제어 자체가 활용형이면(`푹 쉬세요` vs 예문 `쉬시고`) 못 잡는다.
  어미를 벗겨 내는 완화를 넣으면 잡히지만 **그렇게 느슨해진 판정이 애초에 `그러다`를 놓쳤다.**
  전 덱에 3건뿐이라 확인 항목으로 남긴다(`부탁하다`·`푹 쉬세요`·`1도 없다`).
- 🔴 **맞춤법 덱은 검사를 꺼야 한다**(`termIsSpellingPair`). 표제어가 낱말이 아니라 표기
  대조쌍(`돼`/`되`, `-든지`)이라 예문에 그대로 안 나오는 게 정상이다 — 켠 채로 두면 15건이
  오탐으로 뜬다. 같은 ko→ko 라도 **유행어 덱은 표제어가 실제 낱말**이라 `koToKo` 로
  뭉뚱그리면 안 된다.
- 테스트 `__tests__/ko-conjugation.test.ts` 51케이스(전부 실제 사례). 전체 Jest 1,276개 통과.
- 전 덱 진단: 77 → **30건**(예문 중복 18 = 기존 미해결 + 표제어 미사용 12 = B급 9 + 경계 3).

#### ✅ 나머지 22덱 3,287장도 세고 고쳤다 (2026-08-29) — 29장

세야 할 것은 **한국어가 출발어인 덱**뿐이다. 도착어인 44덱 10,376장에서 한국어 문장은
원어 예문의 **번역문**이라, 문법이 어색해도 학습자가 배우는 것은 그쪽이 아니다.

| | 덱 | 장수 |
|---|---|---|
| 한국어가 출발어 | 26 | 6,455 |
| — 사다리 4덱(8/28 완료) | 4 | 3,168 |
| — **나머지(8/29 완료)** | 22 | **3,287** |
| 한국어가 도착어(대상 아님) | 44 | 10,376 |

**표제어 미사용 0건.** 걸린 5건은 전부 오탐이었다(`조금`/`좀` 준말, `TMI`/`tmi` 대소문자,
알려진 한계 3건). 대소문자는 판정기에 반영했다.

**문법 44건 지적 → 29 채택 · 15 기각.** 오류율 **0.6%**(20/3,287, 아래 9건 제외)로
사다리 4덱의 1.0% 보다 낮다 — 소량 덱이라 만들 때 눈검수를 거쳤다는 예상과 맞는다.

#### 🔴 그중 9건은 **8/28 내 수정이 만든 불일치**였다

`Basic Korean 500` 은 en/ja/vi/zh **네 판이 한국어 예문을 공유한다.** 공통 표제어 683개 중
① 문장이 판마다 다른 것은 **딱 3개**였고, 그 3개가 정확히 8/28 에 en 판에서 고친
`밤`·`묻다`·`가르치다` 였다.

```
밤       en: 어젯밤에 잠을 잘 잤어요.          ← 8/28 고침
         ja/vi/zh: 어제 밤에 잠을 잘 잤어요.    ← 그대로 남음
```

🔑 [[project_ko_deck_check_tooling]] 에 **"Basic Korean 500 은 vi/ja/zh 판이 따로 있다"**는
경고가 이미 적혀 있었는데, 사다리 4덱만 보느라 그대로 밟았다. **ko→en 덱을 고칠 때는 같은
표제어의 다른 언어판을 함께 볼 것.** (뜻·번역은 판마다 재생성이지만 **한국어 예문은 복제**다.)

#### 🔴 AI 가 정상을 틀렸다고 한 것이 또 나왔다 (기각 15건의 절반)

- `밥솥에 밥을 안쳤어요` → "앉혔어요"로 고치라 — **`안치다`가 맞다**
- `한국의 주식은 쌀이다` → "밥이다"로 고치라 — **`주식(主食)`이 쌀 맞다**
- `도와주세요` → "도와 주세요" — **`도와주다`가 한 낱말이다**
- `서명해주세요`·`뽑아주세요`·`알려주세요` → 보조용언 띄어쓰기는 **둘 다 허용**(맞춤법 제47항)
- `교수하는` → "가르치는" — `교수하다`는 사전에 있다(문어적일 뿐)

🔑 **두 판정의 교집합도 안전하지 않다.** 위 다섯 중 셋은 교집합에 들어 있었다. 마지막은
사람이 본다.

#### 🔴 맞춤법 덱은 `exampleKr` 이 번역이 아니라 **대조 설명**이다

`❌ '…회수 복용한다.' → ✅ '…횟수 복용한다.'` 형태다. 예문을 고치면 **대조 설명도 같이**
고쳐야 한다. 이번에 3건이 그랬다(`띠다`·`횟수`·`일절`).
그리고 `일절` 예문은 뜻풀이가 말하는 "부정어와 함께"를 **쓰지 않고 있었다** — 뜻풀이와
예문이 어긋난 것이라, 맞춤법 덱은 그 축으로도 볼 만하다(이번엔 안 셌다).

**반영**: 서버 25행 + 번들 29건 + 생성 json 8개 파일 29건. 재조회 대조 서버 25/25 · 번들 29/29.
⏭️ **`travel`·`sports` 4건은 서버에 안 갔다** — 어제 만든 덱 5종이 아직 **시딩 전**이다.
그 5종을 서버에 올릴 때 함께 나간다.

#### ✋ B급 16장은 안 고치기로 했다 — 되살아나지 않게 근거를 적어 둔다

`포함하다` 카드의 예문은 "이 가격에는 세금과 봉사료가 모두 포함**되어** 있어요."다. 학습자는
`포함`을 보고 `-되어`가 피동임을 안다 — **카드가 기능을 못 하는 것이 아니다.** 제보자가
지적한 것도 이 유형이 아니었다(`그러다`→`그렇게`는 어근조차 다른 낱말이라 표제어가 화면에서
사라진다). 능동/피동 짝은 한국어에서 함께 익히는 편이 자연스럽고, 고치려면 예문 16장을
재작성해야 해서 **"예문 재생성 금지" 방침을 정면으로 어긴다** — A 급이 그 예외였던 이유는
기능 불능이었기 때문이고, B 급은 거기 해당하지 않는다.

#### ② 학습 기능 검토 1번 (마지막 학습일)

위 절 참조. **파급 범위부터 세는 것이 먼저다.** 단어를 담기만 해도 "방금 전"이 되는데,
실사용 화면에 보이는 유일한 학습 지표다.

#### ③ 100% 외운 Day 도 학습하고 넘어가야 하지 않나

위 검토 절 참조. 권고는 ⓐDay 칩 3상태 또는 ⓑ재계획 기본 필터 "미암기".
🔴 **잠금 규칙은 건드리지 않는다** — 그게 제보자 버그를 푼 장치다.

### 미해결로 남긴 것

- **온보딩 페이지 인디케이터가 부제 마지막 줄에 겹친다**(네 쪽 전부 · 부제 하단 y=1873,
  점 y≈1883). 1.6.1 이 만든 것이 아니고 출시를 막을 정도도 아니다.
- Y2K 스킨의 Jua 폰트가 안 먹는다 — 하드코딩 `Pretendard_*` **583곳 · 53개 파일**,
  대부분 `StyleSheet.create` 정적 블록 안이라 `useTheme()` 를 못 읽는다. **별도 트랙.**
- `useFonts` 에러 미처리 — 폰트 로딩 실패 시 스플래시에서 영영 멈춘다(기존 위험)
- `evalDay` 의 죽은 50% 와 도움말 문구 불일치
- E5(빈칸 박스 7.5dp) · 동음이의어 병기 축소 — 둘 다 "작아 보이지만 작지 않다"(1.7.0 트랙 6번)
