# 다음 세션 handoff

작성: 2026-07-08 · 대상: 다음 작업 세션 시작 시

이 세션(2026-07-07~08)에서 품사 필터·예문 오디오 작업을 마치고, 다음 세션에 넘길 일들을 정리한다.
연속성 메모리: `project_pos_filter_and_example_audio`.

---

## 🎯 1순위 — CSV PR(#7) 살리기

### 지금 상태 (왜 손봐야 하나)
- PR #7 `feat/csv-import-export` = 단어장 **CSV 내보내기/가져오기** + 같은 언어 쌍 허용.
- 3주 전(6/11~12) 작업이라 main과 벌어짐. **CSV와 무관한 커밋이 섞여 있음**:
  - `chore(release) 1.1.0→1.1.1` (버전 범프) — 불필요
  - `fix(ios) InfoPlist/AppCheckCore` — **이미 main에 cherry-pick됨**(`4953ae0`/`1cd9049`) → 중복
  - 스토어 릴리스노트 문서들 — CSV와 무관
- 그대로 머지하면 충돌/중복 위험.

### 할 일 (살리는 절차)
1. `main` 최신에서 새 브랜치(예: `feat/csv-import-export-v2`)를 판다.
2. PR #7 브랜치에서 **CSV 관련 커밋/변경만** 골라 얹는다(cherry-pick 또는 수동 이식). iOS 빌드·버전·릴리스노트 커밋은 **제외**.
   - CSV 핵심 커밋 후보: `25efdcc`(내보내기/가져오기+같은언어쌍), `b89a9d8`(Android SAF 폴더저장/iOS 공유시트).
3. main 최신과 충돌 해소.
4. 타입체크·lint·jest 통과 확인.
5. 새 PR 생성, 기존 #7은 닫는다.
6. **실기 검증**(빌드 필요) — 아래 체크리스트.

### 설계 메모 (기존 작업 의도, 되살릴 때 유지)
- 내보내기↔가져오기를 단어장 `...` 메뉴에 **한 쌍**으로.
- 가져오기는 별도 라우트 `/import-csv`로 처리 → 모달 레이스 회피.
- CSV 그대로 넣을 땐 `addBatchWords` 재사용 → **AI quota 0**.
- `utils/csv.ts` = RFC4180. ⚠️ `importParser`는 인용처리 안 해서 내보내기용으로 재사용 불가.
- ⚠️ `WordSaveSchema`가 제어문자 금지(NO_CONTROL) → **import 시 제어문자 sanitize 필수**.
- add-word "자동완성으로 추가" 개명도 이 작업에 포함.

### 실기 검증 체크리스트 (기기 빌드에서만 확인 가능)
- [ ] iOS 공유시트로 CSV 내보내기
- [ ] Android SAF 폴더 저장
- [ ] 파일 선택기(picker)로 가져오기, iOS 모달 레이스 없음
- [ ] **엑셀에서 열 때 한글 안 깨짐(BOM)**
- [ ] 같은 언어 쌍 단어장 picker 허용
- [ ] 제어문자 든 CSV 가져와도 저장 실패 없음(sanitize)

---

## 🎯 2순위 — 실기 빌드로 열린 PR 검증 후 머지

빌드 한 번 돌리는 김에 아래 둘도 같이 실기 확인:

### PR #17 `feat/pos-filter-custom-study` (품사 필터)
- [ ] 맞춤학습: 범위(단어장/일차/단어필터) 바꿀 때 품사 칩이 갱신되는지, 선택 품사 사라지면 자동 '전체' 해제
- [ ] 맞춤학습: 품사 골라 학습 시작 시 실제 그 품사만 나오는지
- [ ] 검색: 질의 없이 품사만 골라도 결과 뜨는지(브라우징)
- [ ] 검색: 품사 칩 줄이 단어장 필터 줄과 안 겹치는지
- 로직은 유닛테스트 완료(`__tests__/pos.test.ts` 15개, jest 342/342). UI만 남음.

### PR #15 `feat/flashcard-example-audio` (예문 오디오)
- [ ] 플래시카드 답면 예문 스피커 버튼 재생
- [ ] 오토플레이: 단어→예문 순서로 재생, **긴 예문이 끝까지** 재생 후 다음
- [ ] 오토플레이: 예문 재생 중 일시정지/스킵/이전 시 소리 즉시 끊김(엉킴 없음)
- [ ] 설정 "예문도 읽기" 토글 on/off 반영

이상 없으면 #17 → #15 순으로 머지.

---

## 참고 — 현재 브랜치/PR 상태 (2026-07-08 기준)
- `main`: #11·#13·#14 머지 반영, 원격 동기화됨.
- 열린 PR: #17(품사필터), #15(예문오디오), #7(CSV).
- 로컬 브랜치: 위 3개 PR과 1:1. 머지·폐기된 브랜치는 정리 완료.
