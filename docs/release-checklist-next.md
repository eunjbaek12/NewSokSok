# 다음 정기 릴리스에 묶어야 할 일

기준일: 2026-08-15 · 1.5.0 출시 준비 완료 시점

여기 있는 두 건은 **앱 릴리스와 함께여야 의미가 있다.** 서버만 먼저 바꾸면 출시된 앱과
어긋나고, 앱만 바꾸면 효과가 없다. 2026-08-13에 실제로 그 사고가 났다(아래 1번 배경).

---

## 1. AI 쿼터/광고 정책 전환

### 지금 상태 (2026-08-14 되돌려 둠)

서버는 출시판 1.4.0이 기대하는 정책으로 돌고 있다.

| 항목 | 지금(임시) | 전환 후(목표) |
|---|---|---|
| Free 일일 | 100 | 50 |
| 신규 24시간 | 300 | 300 |
| 게스트(익명) | 분기 없음 | 10 |
| Pro | 1,000/일 · 월 상한 없음 | **3,000/월**(결제일 앵커) · 일일 상한 없음 |
| 캐시 히트 | 쿼터 무차감 | 차감 |
| 한도 초과 | 429 → 보상형 광고 모달 | 뜻만 주는 basic 응답 |

### 왜 되돌렸나

`20260813000000`이 새 정책을 서버에만 적용했는데, 그 정책과 짝이 되는 앱이 아직
심사·배포 전이었다. 특히 `grant_rewarded_bonus`의 시그니처가
`(uuid,integer,integer)` → `(uuid)`로 바뀌며 **옛 시그니처를 drop**해서, 출시된 앱의
`RewardedAdModal`이 "함수 없음"으로 실패했다 — **광고를 끝까지 봐도 단어가 0개.**
8/13부터 8/14까지 그 상태였다.

### ✅ 2026-08-15 — 순서 위험은 해소됐다 (`20260815010000` 적용)

**다음 앱은 `grant_rewarded_bonus`를 1인자로 부른다.** 그 시그니처는 `20260813000000`이
만들었다가 `20260814000000`이 지웠고, **`20260813020000`은 이 함수를 건드리지 않는다** —
즉 원래 절차대로 했으면 앱을 내보내는 순간 보상형 광고가 100% 실패했을 것이다(8/13 사고의
역방향).

`20260815010000_rewarded_bonus_single_arg.sql`로 **1인자를 추가하고 3인자를 그대로 뒀다.**
두 시그니처가 공존하므로 구 앱(1.4.0)과 새 앱이 동시에 정상 동작하고, **릴리스 순서가
더 이상 문제가 되지 않는다.** 정책 값은 현행 그대로다(광고 1회 +50, 일일 상한 200).
`get_ai_quota_status`에 앱이 읽는 `reward_amount`·`reward_views`·`reward_max_views`·
`ad_free_until`도 함께 채웠다 — 안 주면 앱이 "+20"이라 잘못 표시하고, "24시간 배너 제거"
약속이 거짓이 된다.

### 전환에 필요한 조각 — 2026-08-15 실사

🔴 **Edge 는 `enrich-word` 하나가 아니라 3함수다.** `consume_ai_quota` 를 부르는(= 한도를
깎는) 함수가 셋이고, 그중 **둘은 새 정책 코드가 아예 없다** — 되돌리는 게 아니라 새로
짜야 한다. 이 문서가 처음에 2번 항목으로 `enrich-word` 만 적었던 것은 범위 누락이다.

| 조각 | 상태 | 내용 |
|---|---|---|
| `20260813020000_pro_3000_monthly_pool.sql` | ✅ 작성 · ⏳ 미적용 | 한도·티어 (Free 50 · 게스트 10 · Pro 월 3,000) |
| `20260815020000_rewarded_bonus_new_policy.sql` | ✅ 작성 · ⏳ 미적용 | 보상 지급을 `ai_effective_plan` 기반으로 (+20 · 2회) |
| `enrich-word` | ✅ 작성(2026-08-16) · ⏳ 미배포 | 캐시 히트 차감 + 한도 초과 시 basic |
| `scan-image` | ✅ 작성(2026-08-16) · ⏳ 미배포 | 추출 오버헤드 5 제거 (단, 잔량 확인은 남긴다 — 아래) |
| `generate-words` | ✅ 작성(2026-08-16) · ⏳ 미배포 | 요청 N 보다 적게 오면 차액 환불 |

`scan-image` 는 `EXTRACT_COST` 가 0 이었던 커밋이 이력에 없고(`git log -S`), `generate-words`
는 실패 시 전액 환불만 있다. **1.5.0 앱은 이 둘이 이미 반영된 것으로 안내한다** — `3943e3c`
가 FAQ 에서 "사진 장당 5 오버헤드"를 걷어내고 차감 규칙을 "AI 가 채워준 단어 수만큼" 한
문장으로 통일했다. 게다가 `PhotoImportWorkflow` 가 `limit + bonus - used` 로 페이지를
자르므로, 서버가 몰래 5 를 더 떼면 계산이 어긋나 뒷장 몇 개가 조용히 실패한다. **게스트
한도 10 에서 5 는 절반이다.**

### Phase 0 — ✅ 완료 (2026-08-16, 배포·push 는 안 했다)

**① `enrich-word/index.ts`** — 새 정책을 **손으로 병합**한다.

- [x] 참고 원본은 `git show 4f76395^:supabase/functions/enrich-word/index.ts`
  - 🔴 `git revert 4f76395` 금지 — Edge 와 마이그레이션을 함께 바꾼 커밋이라 이미 적용된 `20260814000000` 파일이 사라진다
  - 🔴 `git checkout 4f76395^ -- <path>` **도 금지** — 그 뒤 `d71f4d7`(8/15, 폭주 차단 v43)이 같은 파일에 얹혔다. 통째로 덮으면 `runawayFieldOf` 가 날아간다
  - 🔑 더 앞의 `482ab4a`(정책 최초 구현)가 아니라 **`4f76395^` 를 볼 것.** 그 사이에 `69c6e4f` 가 `PROMPT_VERSION` 을 8 → 7 로 되돌려 놨다. `482ab4a` 를 베끼면 8 이 딸려 와 **캐시 8만 행이 통째로 무효**가 된다
- [x] 캐시 조회를 quota 차감 **뒤로** — 캐시 히트도 차감
- [x] 한도 초과 시 basic(뜻만) 응답은 **`mode === 'autocomplete'` 일 때만**
  - 🔴 8/13 원본엔 mode 조건이 없어 photo 까지 뜻이 채워진다. 스캔·생성은 **429 를 유지**해야 광고·Pro 압력이 산다
- [x] full 캐시 조회는 `enrichment_level === 'full'` 로만 히트 판정, basic 결과는 `'basic'` 으로 저장
  — 뜻만 결과가 캐시에 굳어 Pro 까지 뜻만 받는 것을 막는다. ✅ 컬럼은 서버에 이미 있다(8/15 실측, 마이그레이션 불필요)
- [x] `_shared/gemini-meaning.ts`(`translateMeaningOnly`)는 남아 있다 — 호출부만 다시 켜면 된다
- [x] 8/13 판에 없던 것 두 가지를 얹었다:
  - `runawayFieldOf` 를 basic 경로에도 적용 — 폭주 가드(`d71f4d7`)는 full 경로에만 있었다
  - basic upsert 는 **캐시가 아예 없을 때만** — full 행을 basic 으로 덮으면 그 단어는 Pro 에게도 영영 뜻만 나간다

**② `scan-image/index.ts`** — 추출 오버헤드 제거.

- [x] `consume_ai_quota(cost=5)` → **`get_ai_quota_status` 로 잔량 확인만**, 0 이면 429
  - 🔑 Pro 는 `day_limit == month_limit == 3000` 이라 일일 잔량만 보면 월 소진자를 못 막는다 → **월 잔량도 함께** 본다
- [x] 실패 시 환불 코드도 함께 제거 (차감이 없으니 환불할 것도 없다)
- [x] 헤더 주석의 "장당 고정 5"도 수정
  - 🔴 **차감만 0 으로 두고 확인을 빼면 안 된다** — 한도를 다 쓴 사용자가 vision 을 무한히 부를 수 있다. rate limit 은 분당 제한이라 하루 총량을 못 막는다

**③ `generate-words/index.ts`** — 차액 환불.

- [x] `result.length < wordCount` → `refund_ai_quota(wordCount - result.length)`
  - ⚠️ 클라이언트 검증에서 떨어진 몫까지 환불하려면 클라가 개수를 보내야 하는데, 그건 한도를 무한히 얻는 구멍이 된다. **서버가 스스로 아는 범위까지만.**
  - 응답의 `quota.used`(·Pro 는 `month_used`)도 환불분만큼 되돌린다 — 차감 직후 값을 그대로 주면 앱 표시와 서버가 어긋난다

- [x] `pnpm test` · `pnpm run lint` — 981 통과. 실패는 전부 기존 것(수트 3개 실행 실패, lint 15 에러 모두 hex 색·import 규칙)이고 Edge 3함수는 하나도 없다
  - ⚠️ **Deno 타입 검사는 못 했다**(로컬에 Deno CLI 없음). 문법·타입 검증은 `functions deploy` 시점이 처음이다

**🔑 착수 전 확인해 둔 것 — 앱은 이미 새 정책을 받을 준비가 돼 있다**

- `enrichment_level: 'basic'` 을 1.5.0 이 이해한다 → `app/add-word.tsx:371` 이 "뜻만 채웠다 + AI 로 채우기" 안내를 띄운다
- **basic 결과가 기기에 굳어 영구히 뜻만 나오는 사고는 없다.** `lib/enrich-cache.ts` 는 레벨을 안 보고 저장하지만, `lib/translation-api.ts:88` 이 `apiKey ? getCachedEnrich(...) : null` 이라 **운영자 경로는 로컬 캐시를 아예 조회하지 않는다**(BYOK 전용). BYOK 사용자는 한도가 없어 basic 을 받을 일이 없다
- 한도 초과 basic 응답의 `quota` 는 `used >= limit + bonus` 상태로 나가고 `applyEdgeQuota` 로 store 에 반영된다 → 다음 호출은 `hooks/useAddWord.ts:213` 의 사전 검사에서 광고 모달로 간다
- ⚠️ 남는 구멍: 한도 초과 사용자의 basic 호출은 **차감이 없어** 서로 다른 단어로 무한히 부를 수 있다(원가는 flash-lite 극소, 분당 rate-limit 은 걸린다). 8/13 판에서 물려받은 성질이라 이번에 바꾸지 않았다

### Phase 1 — 게이트: 넘기 전엔 서버에 아무것도 올리지 않는다

- [x] 1.5.0 이 **두 스토어 모두** 공개 (Play 단계적 출시면 100% 인지) — 2026-08-16 확인
- [ ] 즉시 전환할지, 채택률이 오를 때까지 며칠 둘지 판단

리워드 시그니처 문제는 `20260815010000` 로 해소됐지만(위 참조), **구 앱이 무사한 것과
열화가 없는 것은 다르다.** 1.4.0 잔존 사용자에게 남는 어긋남:

- 한도 축소(100 → 50) — 업데이트 안 한 사용자의 한도가 그 시점에 줄어든다
- 캐시 히트 차감 — 체감 한도가 더 빨리 닳는다
- ⚠️ **한도 초과 시 429 대신 basic 200** → 1.4.0 은 `enrichment_level` 을 모르니 이걸 정상
  응답으로 취급한다. **보상형 광고 모달이 안 뜬다.** 8/13 사고의 약한 버전이라, 이 항목
  때문에라도 채택률이 어느 정도 오른 뒤가 안전하다.

### Phase 2 — 서버 전환 (순서 고정: 되돌리기 어려운 쪽 먼저)

1. **적용된 마이그레이션 목록을 먼저 눈으로 확인한다** — `db push` 는 미적용분을 **전부** 올린다.

   ```bash
   npx -y supabase@latest db query --linked \
     "select version from supabase_migrations.schema_migrations order by version desc limit 8;"
   ```

2. **마이그레이션 두 개를 함께 적용한다.** 하나만 적용하면 어긋난다.

   🔴 `20260813020000` 은 `grant_rewarded_bonus` 를 건드리지 않는다. 그것만 적용하면
   앱은 "+20단어"라 표시하는데 서버는 `20260815010000` 이 박아 둔 +50 을 준다.

   🔴 **`--include-all` 을 붙일 것.** 2026-08-15에 `20260815000000`·`20260815010000` 을
   단독 적용해 **타임스탬프가 역전돼 있다** — 그냥 `db push` 하면 `20260813020000` 을
   **exit 0 으로 조용히 건너뛴다.** 적용 여부는 `migration list` 로 눈으로 확인할 것.

3. **1인자·3인자 `grant_rewarded_bonus` 가 둘 다 남아 있는지 확인한다.**

   ```sql
   select pg_get_function_identity_arguments(oid) from pg_proc where proname = 'grant_rewarded_bonus';
   ```

   🔴 **3인자를 지우지 말 것.** 앱 업데이트에는 시차가 있어 1.4.0 이 한동안 남는다.
   ⚠️ 3인자는 호출자가 준 +50 을 그대로 주므로 새 정책보다 후하다. 일부러 둔다 —
   구 앱 UI 가 "+50단어"라고 적혀 있어 서버만 깎으면 표시와 실제가 어긋난다.

4. **Edge 3함수를 배포한다.**

   ```bash
   npx -y supabase@latest functions deploy enrich-word generate-words scan-image --linked
   ```

   ⚠️ `_shared/gemini-vertex.ts` 를 건드렸다면 그 파일을 번들하는 **`seed-enrich` 까지 4개**를
   함께 배포해야 한다.

### Phase 3 — 검증 (실기)

- [ ] `select * from ai_effective_plan('<게스트 uuid>')` → `guest` · 10 · 보상 10·1회
- [ ] 로그인 신규 24h → 300, 그 뒤 → 50, Pro → 3,000/3,000
- [ ] 게스트로 사진 스캔 → 10장에서 멈추고 광고 모달, **5 오버헤드 없음**
- [ ] 자동완성 한도 초과 → **뜻만 나오고 예문 없음**(429 아님)
- [ ] AI 단어 생성 한도 초과 → **429 + 광고 모달**(basic 이 아니다)
- [ ] 같은 단어 재조회(캐시 히트) → 한도가 줄어든다
- [ ] 보상형 광고 시청 → +20 반영 (게스트는 +10 · 1회)

🔑 **게스트 계정으로 검증하면 10번 만에 한도에 닿는다.** 로그인 계정은 신규 24h 300이라
현실적으로 못 채운다. 1.5.0(`3943e3c`)부터 게스트도 스캔·생성을 쓸 수 있으므로 세 경로를
모두 게스트로 잴 수 있다 — 그 이전에는 자동완성 하나뿐이었다.

### 롤백

Edge 는 이전 버전 재배포로 즉시 되돌아간다. 마이그레이션은 `20260814000000` 같은 되돌림
파일을 따로 써야 한다 — 그래서 **Edge 를 마지막에 배포**해, 문제가 나면 그쪽부터 되돌린다.

### 참고

- `db push`는 미적용 마이그레이션을 **전부** 올린다. 특정 파일만 적용하려면
  `db query --file <migration>` 으로 실행하고 `migration repair --status applied <version>`
  으로 이력만 등록한다(8/14에 이 방법을 썼다).
- Supabase CLI 는 `.env` 를 자동 로드한다 — 자리표시자 한 줄이 `login` 까지 막는다.
- CLAUDE.md의 티어 표는 **목표 정책**이며 현재 서버와 다르다는 주석이 달려 있다.

---

## 2. 단어 언어쌍 오염 — 로컬 SQLite 마이그레이션 ✅ 구현 완료(2026-08-15)

`lib/db/migrations/019_fix_curated_word_languages.ts` (SCHEMA_VERSION 18 → 19).
실제 SQLite로 6가지 경우를 검증했다 — 오염/정상/사용자 리스트/삭제된 단어/NULL 언어,
그리고 재실행 시 대상 0건(멱등). **남은 것은 실기 검증뿐이다**(아래 §확인할 것).

### 무엇이 잘못돼 있나

`features/vocab/db.ts`의 `createCuratedList`가 단어 언어를 리스트 언어가 아니라
`'en'/'ko'` 하드코딩으로 스탬프하고 있었다. **원인은 PR #43(2026-07-09 머지)에서 이미
고쳤고 신규 저장은 정상**이다. 남은 것은 그 이전에 저장된 데이터다.

클라우드 실측(2026-08-14): **8,275행 · 21개 리스트 · 사용자 11명.**

| 리스트 실제 언어 | 단어에 찍힌 값 | 행 |
|---|---|---|
| ko>en | en>ko | 6,065 |
| zh>ko | en>ko | 1,491 |
| ja>ko | en>ko | 497 |
| es>ko | en>ko | 122 |
| ko>ko | en>ko | 99 |

찍힌 값이 예외 없이 `en>ko`라 하드코딩 흔적과 정확히 일치한다.

### 고치는 법

**로컬 SQLite 마이그레이션 하나면 된다.** 조건:

```
isCurated = true 인 리스트에 속하고,
단어의 (sourceLang, targetLang) ≠ 리스트의 (sourceLanguage, targetLanguage)
→ 리스트 값으로 덮는다
```

- 로컬을 고친 뒤 **dirty로 직접 마킹해야** push로 서버까지 맞춰진다. 🔴 이 문서가 처음
  "고치면 dirty로 표시된다"고 적은 것은 **틀렸다** — dirty 집합은 SQLite가 아니라
  AsyncStorage 기반 Zustand store(`features/sync/store.ts`)라, SQL만 돌리면 로컬만
  바뀌고 서버는 영영 그대로다. 마이그레이션이 `markWordsDirty(ids)`까지 부른다.
- **게스트도 고쳐진다** (서버 UPDATE는 게스트에 닿지 않는다).
- `cloud_words.updated_at`을 인위로 올리지 않으므로 **운영 지표가 왜곡되지 않는다**
  (`docs/ops-analytics-queries.md` Q1이 그 컬럼으로 "오늘 활동한 사람"을 판정한다).

### 건드리지 말 것

사용자가 직접 만든 리스트(`isCurated = false`) 284행에도 일본어·한자가 들어 있지만,
리스트 자체가 `en>ko`라 무엇이 맞는지 기계적으로 알 수 없다. 사용자가 리스트 언어를
잘못 고른 것일 수도 있다.

### 기대 효과는 크지 않다 (2026-08-14 실측으로 확인)

처음에는 "AI 보강이 올바른 언어로 동작한다"를 근거로 들었으나 **성립하지 않는다.**
대상 8,275행이 **전부 이미 뜻·예문·발음을 갖고 있다**(큐레이션 덱 원본). AI 보강을
부를 일이 없고, 예문 자동 보강도 `!w.exampleEn`일 때만 작동해 타지 않는다. 해당 11명
중 AI를 쓴 사람은 1명뿐이다.

실제로 남는 효과는 이것뿐이다:

- **단어장 카드의 언어쌍 표시가 맞아진다** — `deriveDisplayLanguages`가 단어 최빈 언어를
  우선하므로 지금은 중국어 덱이 `EN → KO`로 보인다. 원래 신고된 증상이 이것이다.
- 단어 편집 화면, 네이버 사전 링크(지금은 영어 사전으로 간다)
- 나중에 그 단어로 AI를 부를 때

TTS는 `list.sourceLanguage`를 쓰므로 오염과 무관하게 정상이다.
