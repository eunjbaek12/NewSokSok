# 접근성 — 1.7.0 (10/1) 작업 설계

2026-09-03 · 마감 **9/9** ([[피처링 지명 9/10 제출]] `store-assets/featuring-nomination.md` §0)

---

## §1 왜 지금인가

애플 에디터의 7가지 채점 기준 중 🔴가 둘이다 — **접근성**과 **사용자 경험**.
사용자 경험(= 평점·리뷰)은 8일에 만들 수 없다. **접근성은 만들 수 있다.**
7기준 중 우리 손으로 뒤집을 수 있는 유일한 항목이라 꺼냈다.

⚠️ 목적이 지명 점수만은 아니다. 어휘 앱은 **글자를 크게 쓰는 사용자 비중이 실제로 있다**.
다만 마감이 지명에 걸려 있어 범위를 그 날짜에 맞춰 자른다.

## §2 실측 (2026-09-03)

```
누를 수 있는 것        359 개   ← Pressable + TouchableOpacity
  accessibilityLabel    18 개   (5%)

아이콘 전용 버튼       108 개   ← 안에 <Text> 없이 아이콘만 = VoiceOver 가 "버튼" 이라고만 읽는다
  라벨 있는 것           9 개
  🔴 라벨 없는 것       99 개 · 30 개 파일

fontSize 리터럴        636 개   ← FontSize 토큰은 16 개뿐
고정 height            198 개 · 53 개 파일
lineHeight 리터럴       83 개
numberOfLines          106 개
```

🔑 **이 숫자가 범위를 결정한다.** 글자가 있는 버튼은 VoiceOver 가 그 글자를 읽으므로
라벨이 없어도 최소한 동작한다. 문제는 **아이콘만 있는 99개**다 — 이것만 채우면
"모든 컨트롤에 이름이 있다"가 성립한다.

반면 Dynamic Type 은 **fontSize 636 + 고정 height 198** 을 손대야 한다. 5일에 불가능하고,
무리하면 레이아웃 회귀를 대량으로 만든다.

## §3 하는 것 [선택 불필요]

### A. VoiceOver 라벨 99개 — 대표 흐름 순

| 순 | 대상 | 미달 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | `app/(tabs)/index.tsx` · `vocab-lists.tsx` · `_layout.tsx` | 5 | 앱을 열면 처음 닿는 화면 |
| 2 | `app/add-word.tsx` | 8 | 단어를 넣는 주 경로 |
| 3 | `app/list/[id].tsx` | 10 | 단어장 상세 — 학습 진입로 |
| 4 | 학습 5종 — `flashcards` 4 · `quiz` 3 · `examples` 3 · `autoplay` 7 · `plan` 6 | 23 | 앱의 본체 |
| 5 | `features/curation/screen.tsx` | 10 | 덱 탐색 — 에디터가 열어 볼 화면 |
| 6 | `components/WordDetailModal.tsx` · `ManageModal.tsx` · `search-modal.tsx` | 12 | 자주 뜨는 모달 |
| 7 | 나머지 14개 파일 | 31 | 설정·약관·FAQ·워크플로 |

### B. 라벨 문구는 i18n 으로, 공용 키로 묶는다

99개를 낱낱이 번역하면 297개 문자열이 된다. 실제로는 **같은 말이 반복**된다
(닫기·삭제·재생·다음·이전·더보기·뒤로). **공용 키 30~40개로 묶고** 화면 고유한 것만 따로 둔다.

```
a11y.close / a11y.back / a11y.delete / a11y.play / a11y.pause /
a11y.next / a11y.prev / a11y.more / a11y.star / a11y.unstar / ...
```

⚠️ **`accessibilityRole` 도 함께 준다.** 라벨만 있고 역할이 없으면 VoiceOver 가
"두 번 탭하여 실행"을 안내하지 못한다. 아이콘 버튼은 `role="button"`.

### C. Dynamic Type 은 **고치지 않고 재기만 한다**

5일에 636개를 토큰화할 수 없다. 대신 **지금 무엇이 깨지는지 실측**한다.

- 설정 › 손쉬운 사용 › 디스플레이 및 텍스트 크기 › **더 큰 텍스트를 최대**로
- 대표 화면 8개(홈·단어장 목록·단어장 상세·단어 추가·플래시카드·퀴즈·결과·설정)를 열어
  **스크린샷으로 남긴다**
- 결과를 이 문서 §6 에 표로 적는다

🔑 **재는 것 자체가 산출물이다.** 지금은 큰 글자에서 무슨 일이 벌어지는지 **아무도 모른다**.
알고 나면 §6 을 정직하게 쓸 수 있고, 1월 작업의 범위도 그 표에서 나온다.

## §4 하지 않는 것 [근거 있음]

| | 왜 |
|---|---|
| **fontSize 636개 토큰화** | 5일 초과. 무리하면 레이아웃 회귀를 대량 생산한다 |
| **고정 height 198개 제거** | 위와 같음. 토큰화와 한 몸이라 따로 못 한다 |
| **`maxFontSizeMultiplier` 로 상한 걸기** | 🔴 **이건 "지원"이 아니라 "제한"이다.** 깨진 화면은 안 보이게 되지만 사용자 설정을 무시하는 것이라, §5 에 Dynamic Type 을 체크할 근거가 못 된다. **어설픈 지원은 역효과** — 에디터가 실제로 켜서 확인한다 |
| VoiceOver 커스텀 액션·포커스 순서 제어 | 라벨이 먼저다. 라벨 없이 순서를 다듬는 건 의미가 없다 |

## §5 답변지에 무엇을 쓸 수 있게 되나

작업 후 `store-assets/featuring-nomination.md` 를 이렇게 고친다.

| 절 | 지금 | 작업 후 |
|---|---|---|
| §5 기술표 · VoiceOver | ⏳ | **✅** — 모든 컨트롤에 이름과 역할 |
| §5 기술표 · Dynamic Type | ⏳ | **❌ 유지** — 체크하지 않는다 |
| §6 접근성 | 작업 전 실측치 | VoiceOver 결과 + Dynamic Type **측정 결과표** |
| 전제조건 표 · 접근성 | 🔴 미확인 | 🟡 — 🟢 아님. 한 축만 끝났다 |

🔑 **🟢가 아니라 🟡로 적는 것이 이 설계의 정직한 결론이다.** 7기준 중 하나를 🔴에서
🟡로 올리는 것이 5일로 살 수 있는 전부다. 🟢는 Dynamic Type 까지 끝나는 1월이다.

## §6 Dynamic Type 측정 결과 (작업 후 채울 것)

| 화면 | 최대 글자 크기에서 | 스크린샷 |
|---|---|---|
| 홈 | | |
| 단어장 목록 | | |
| 단어장 상세 | | |
| 단어 추가 | | |
| 플래시카드 | | |
| 퀴즈 | | |
| 학습 결과 | | |
| 설정 | | |

## §7 검증

- **실기**: iOS 실기에서 VoiceOver 를 켜고 §3-A 의 흐름 1~4 를 손끝으로 훑는다.
  "버튼"이라고만 읽히는 곳이 남아 있으면 실패.
  ⚠️ 시뮬레이터가 아니라 실기 — 이전에 *"dev 에서 확인했다"가 검증이 아니었던* 전례가 있다.
- **자동**: 아이콘 전용 버튼에 라벨이 없으면 걸리는 소스 검사 테스트.
  🔴 **주석까지 훑지 않게 할 것** — 소스 검사 테스트가 주석 이력에 걸린 전례가 있다.
- **Android**: TalkBack 도 같은 속성을 읽는다. iOS 를 먼저 맞추고 한 바퀴 확인.

## §8 일정

```
9/5   흐름 1~2  (홈·탭·단어 추가)          13개 + i18n 공용 키 설계
9/6   흐름 3~4  (단어장 상세·학습 5종)      33개
9/7   흐름 5~6  (큐레이션·모달 3종)         22개
9/8   흐름 7    (나머지 14개 파일)          31개
9/9   Dynamic Type 측정 8화면 · 실기 VoiceOver 검증 · 답변지 §5·§6 갱신
9/10  ★ 지명 제출
```

⚠️ **밀리면 흐름 7을 버린다.** 설정·약관·FAQ 는 에디터도 사용자도 마지막에 본다.
흐름 1~6(74개)까지가 "대표 경로는 다 읽힌다"의 최소선이다.

⚠️ 이 작업은 **플래그로 가리지 않는다** — 중간 릴리스에 먼저 나가도 무해하고,
오히려 **9/10 지명 시점에 이미 라이브인 편이 유리하다**(에디터가 스토어에서 받아 확인).
